# HyperMixx 实现方案 — Rust + Slint DJ 软件

> 调研日期：2026-08-13（v2，按确认后的目标修订）。参考项目：`mixxx`（成熟 C++ DJ 软件）、`mixi`（实验性 Rust/WASM DJ 项目）。

## 0. 目标与原则（已确认）

| 项 | 决策 |
|---|---|
| 目标平台 | **香橙派 4 一体机（RK3399, aarch64）+ Linux PC**，架构保持跨平台（未来 Windows/macOS） |
| 用途/许可证 | 开源、可能商用 → 自研代码 **MIT OR Apache-2.0**；依赖栈全选宽松许可，**零 GPL 传染** |
| 起步规模 | 2 deck（引擎按 N deck 设计） |
| 音频规格 | 引擎采样率 48kHz，256 帧起步 |
| 第一原则 | **轻量、性能优先；每个模块有 trait 缝，可替换实现** |
| 测试音频 | `/home/windupbird/Workspace/HyperMixx/yehno - Always.flac`（24bit / 44.1kHz / 立体声 / 5:29） |
| mixi 代码 | **不复制**（PolyForm-Noncommercial，商用不可用），只借鉴算法思路，全部重写 |

## 1. 调研结论

### 1.1 从 mixxx 借鉴的设计（不抄代码，抄架构）

| # | 设计 | mixxx 位置 | 对 Rust 版的映射 |
|---|---|---|---|
| 1 | **控制总线**（ControlObject：`(group,item) → 线程安全共享值`，UI/MIDI/引擎唯一通信面） | `src/control/controlobject.h` | `Arc<ControlValue>`（seqlock/arc-swap）+ watch 通知 |
| 2 | **解码与实时分离**：解码 worker → 分块缓存 → 音频线程只读缓存 | `src/engine/cachingreader/` | symphonia worker + SPSC chunk ring buffer |
| 3 | **变速引擎抽象**：`EngineBufferScale`（linear / SoundTouch / RubberBand），重处理放 worker 线程池 | `src/engine/bufferscalers/`、`rubberbandworkerpool` | `Keylocker` trait + 异步预处理器 |
| 4 | **FX 框架**：不可变 `EffectManifest` → 实例化 `EffectProcessor`，GUI↔引擎走无锁消息管道 | `src/effects/backends/effectmanifest.h`、`src/engine/effects/` | Rust trait `Effect`（manifest + instantiate），参数平滑 per-channel |
| 5 | **分析管线**：流式 `Analyzer` 接口 + 插件列表 + 优先级线程池 | `src/analyzer/`、`trackanalysisscheduler` | `trait Analyzer` + rayon（Pi 上限 1~2 线程） |
| 6 | **修订版 SQLite schema**：迁移历史 + `track_analysis` BLOB 存分析结果 | `res/schema.xml`、`schemamanager.cpp` | rusqlite + 迁移目录 |
| 7 | **外部曲库导入**：Rekordbox PDB 二进制解析、Serato crate V2 + ID3 内嵌 beatgrid/cues | `lib/rekordbox-metadata/`、`src/library/serato/`、`src/track/serato/` | rekordcrate 或自研；Serato 自研 |
| 8 | **MIDI 层只做 bytes↔controls**，绝不直接碰引擎 | `src/controllers/midi/midicontroller.cpp` | midir 线程 → 映射表 → 控制总线 |
| 9 | **波形 RGB 数据模型**：每视觉列存 low/mid/high/all 四带，overview/detail 两级 | `src/waveform/waveform.h`、`analysisdao` | 同样数据模型，存 SQLite BLOB |

### 1.2 从 mixi 借鉴（⚠️ 许可证，见 §0）

- **DSP 算法结构**（`mixi-core/src/dsp/`）：三段 EQ（RBJ biquad）、delay/reverb/flanger/phaser/gate/失真、true-peak + lookahead limiter、参数平滑器、block-rate LFO 系数。**全部按算法思路重写，不复制代码。**
- **调性检测**（`key.rs`）：Goertzel 色度图 + Krumhansl-Kessler + Camelot 轮，零依赖，思路清晰。
- **beat math**（`automix.rs`）、**相位对齐/自动 cue**（`pll_analysis.rs`）、**DVS 时间码**（`dvs/`，后期功能参考）。
- 工程模式：零分配热路径、预分配 scratch buffer、denormal 治理（native 开 FTZ 更简单）、参数版本校验。
- **不做**：它的浏览器/Electron/React 层、wasm ABI、SharedArrayBuffer ring（wasm 无原子语义）。

### 1.3 Rust 生态选型（crates.io 实测，2026-08，含许可证核实）

| 用途 | crate | 版本 | 许可证 | 备注 |
|---|---|---|---|---|
| 音频输出 | cpal | 0.18.1 | Apache-2.0 | ALSA/JACK/PipeWire/WASAPI/CoreAudio |
| 解码 | symphonia | 0.6.0 | MPL-2.0 | 纯 Rust；**feature 裁剪只留 MP3/AAC/FLAC/OGG/WAV** |
| 采样率转换 | rubato | 5.0.0 | MIT/Apache | 纯 Rust，44.1→48k 曲目预处理 |
| **keylock 首选** | **timestretch** | 0.11.0 | MIT | 纯 Rust，"optimized for EDM"——M0 实测定案 |
| keylock 备选 | signalsmith-stretch | 0.1.3 | MIT | Signalsmith Stretch（音频工业界口碑好） |
| keylock 备选 | rubato 两级法 | 5.0.0 | MIT/Apache | 变速×音高解耦 |
| keylock 参考 | bungee-rs | 0.2.0 | 包装 MPL，**底层 Bungee 为 GPL/商业** | Rubber Band 作者出品；**仅基准对比，默认排除** |
| EQ/滤波器 | biquad | 0.6.0 | MIT/Apache | RBJ cookbook |
| FFT | realfft / rustfft | 3.5.0 / 6.4.1 | MIT | 分析用 |
| MIDI IO | midir | 0.11.0 | MIT | midly 0.5.3 做文件解析 |
| 标签元数据 | lofty | 0.25.0 | MIT/Apache | ID3/MP4/Vorbis/APE |
| 曲库 | rusqlite | 0.40.2 | MIT | bundled SQLite |
| UI | Slint | 1.17.1 | **GPLv3 或商业，另有免费 Royalty-free 许可**（以 slint.dev 官网为准） | 见 §1.4 |
| 无锁共享 | ringbuf / seqlock / arc-swap | 0.5.1 / 0.2.0 / 1.9.2 | MIT/Apache | 控制总线与音频缓冲 |
| Rekordbox | rekordcrate | 0.3.0 | MPL-2.0 | 下载量低需评估；备选参考 mixxx kaitai spec 自研 |

**默认依赖栈全为 MIT/Apache/MPL——无 GPL 传染**，商用无忧。**不引入**：rodio（直接用 cpal）、fundsp（重，biquad 足够）、FFmpeg 等 C++ FFI（ARM 交叉编译痛）。

### 1.4 Slint 许可证说明（必须处理）

Slint 1.x 采用 **GPLv3 或商业双许可**，并向闭源桌面产品提供免费 **Royalty-free 许可**（需在 slint.dev 注册获取，具体条款以官网为准——大陆网络访问 slint.dev 超时，需自行核实）。

对本项目的影响：
- 若项目以 **GPLv3** 发布：直接用 Slint，无额外成本。
- 若项目以 **MIT/Apache** 发布：闭源/商业分发时二进制会与 GPLv3 冲突，需要取得 Slint Royalty-free 或商业授权。

**建议**：代码先按 MIT/Apache 写，Slint 授权问题在第一次公开发布前解决（二选一：转 GPLv3 发布 / 申请 Royalty-free）。UI 层把 Slint 封装成薄接口（`UiBackend` trait 之后加）可降低换 UI 的沉没成本，但 V1 不为此过度设计。

## 2. 总体架构

```
┌────────────────────────────────────────────────────────┐
│ UI 层（Slint，主线程）                                    │
│  皮肤 / 波形(RGB) / 曲库浏览器 / 混音台 / MIDI 设置        │
└────────────────────────┬───────────────────────────────┘
                         │ 读写（唯一通信面）
┌────────────────────────▼───────────────────────────────┐
│ 控制总线 ControlBus（全项目共享）                          │
│  [Deck1].play  [Deck1].rate  [Deck1].eq_high             │
│  [Deck1].bpm  [Master].crossfader  [EffectRack1]...      │
│  每项 = Arc<Control>：seqlock 保护的值 + watch 变更通知    │
└────────┬───────────────────────────────┬────────────────┘
         │ 引擎每块读取参数                  │ MIDI 层只做 bytes→controls
┌────────▼───────────────────────────────┐┌───────────────▼┐
│ 音频引擎（实时线程，零分配）              ││ MIDI 层（midir   │
│ Deck A/B:                              ││ 输入线程）       │
│  缓存读取 → 变速/keylock → EQ → FX →    ││ 消息→映射表→     │
│  音量 → crossfader → master limiter     ││ ControlBus.set  │
│ Beat Clock（leader/follower 同步，M2）  ││ LED 反馈输出    │
└────────┬───────────────────────────────┘└─────────────────┘
         │ chunk ring（SPSC，预解码缓存）
┌────────▼───────────────────────────────┐
│ 服务层（普通线程池，Pi 上限 1~2 条）      │
│  CachingReader worker：symphonia 解码    │
│    + rubato 转到 48k → 写入缓存          │
│  分析线程：BPM / key / 波形RGB           │
│  曲库扫描 / 外部库导入 / SQLite DAO       │
└─────────────────────────────────────────┘
```

### 2.1 核心决策

1. **控制总线是一等公民**（mixxx 最值得抄的一点）。UI、MIDI、引擎互不直接调用，全部经 `(group, key) → Arc<Control>`。引擎在音频回调开头批量快照所需参数（每块一次，无锁读），UI/MIDI 写走 seqlock。MIDI 映射、皮肤绑定、将来的脚本化都免费获得。
2. **解码永远不在实时线程**。worker 用 symphonia 解码 + rubato 一次性重采样到 48kHz，按 2048-sample chunk 写入每 deck 的 SPSC ring（**有界，每 deck ≤ 8MB**，树莓派内存友好）。实时线程只做：读 chunk → 变速 → EQ/FX → mix。
3. **keylock（变速不变调）用异步预处理 + 交叉淡化**（mixxx RubberBandWorkerPool 模式）：audio 线程对当前 pitch/tempo 组合直接变速播放，同时 worker 用 time-stretch 引擎预渲染正确音高的流，就绪后在块边界交叉淡化切过去。避免 phase-vocoder 卡实时线程，也允许用重质量引擎。`Keylocker` trait 抽象算法，M0 用实际测试曲做基准定默认（timestretch 优先，纯 Rust 对 aarch64 交叉编译最友好）。
4. **FX 框架**：不可变 manifest（id、参数声明、默认值）→ `fn instantiate(sr) -> Box<dyn EffectProcessor>`。每 deck 一个 FX rack（4 槽），引擎侧 per-channel 参数平滑。V1 内置：filter 扫频、echo、reverb、flanger、phaser、gate、失真。算法参考 mixi dsp/ 重写，EQ 用 biquad crate。
   **协议（已按 M3 实测定案）**：`process(&mut out, frames, ctx)` 逐块原位只出湿声、`set_params(&[f32;4])` 每块快照（内部 clamp、幂等、廉价）、`reset()` 清状态；干湿混由 rack 统一（10ms Smoother，settled+target 0 整槽跳过 = 旁路冻结）；`FxContext`（块首绝对拍/拍内相位/拍周期/网格有效位）在 process 内计算（sync 可能在 update_params 与 process 之间改 rate）；换型是音频线程唯一分配点（deck 按 fx_type 快照去抖并写回 manifest 默认值）。
5. **Beat Clock 同步（M2）**：单一时钟（f64 相位 + BPM），leader/follower 协商（mixxx EngineSync 思路）。跟随者速率 = tempo 比 × (本曲 BPM / leader BPM)，相位误差在 quantize 边界校正，采样域计算，天然 sample-accurate。
6. **分析结果进 SQLite BLOB**（mixxx `track_analysis` 模式）：波形 overview（~1024 样本/列）与 detail（~256 样本/列）两级，每列 4×u8 = low/mid/high/all。

### 2.2 模块可替换清单（trait 缝）

| 模块 | trait | 默认实现 | 可替换为 |
|---|---|---|---|
| 音频输出 | `AudioBackend` | cpal（ALSA/JACK/PipeWire） | jack crate 直连、WASAPI（Win） |
| 解码 | `Decoder` | symphonia | 逐格式解码器、FFmpeg FFI |
| 重采样 | `Resampler` | rubato | libsamplerate FFI |
| keylock | `Keylocker` | timestretch（M0 定案） | signalsmith-stretch、rubato 两级、自研 WSOLA |
| BPM 检测 | `BeatDetector` | 自研 onset flux + comb | open-bpm（git 依赖） |
| 调性检测 | `KeyDetector` | 自研 chromagram + Krumhansl-Kessler | qm-dsp 移植 |
| FX | `Effect`/`EffectProcessor` | 自研内置 | LV2 宿主（后期） |
| 曲库导入 | `LibraryImporter` | rekordbox/serato/vdj 各一 | 逐格式替换、libdjinterop FFI |
| MIDI | `MidiBackend` | midir | 自研 ALSA sequencer |
| 渲染 | Slint 渲染后端特性 | femtovg（默认，Pi 上走 GLES） | skia、软件光栅 |

### 2.3 ARM 单板适配要点（香橙派 4 / RK3399）

- **芯片**：RK3399 = 2×Cortex-A72（大核 ~1.8GHz）+ 4×Cortex-A53（小核），aarch64 big.LITTLE。线程亲和策略：**音频线程 + 解码 worker 绑大核（A72）**，分析线程放小核（A53）+ nice 低优先级（1 条）。实时 DSP（EQ/FX）每样本 biquad 在 ARM 上绰绰有余，关键是 keylock 与渲染——M0 基准覆盖。
- **渲染（重点风险）**：RK3399 的 Mali-T860 靠 Panfrost 驱动，GLES 3.1 能用但性能中等。Slint 默认 femtovg 走 GLES，可能成为瓶颈。M0 就验证：波形纹理只更新脏区域 + 两级 LOD；不够则降波形分辨率/帧率或切软件渲染，skia（重）作最后手段。
- **音频设备**：一体机用 USB 声卡 / I2S DAC（ES9023、PCM5102 等），经 ALSA（cpal）统一覆盖；JACK 仅 PC 调试用。
- **存储**：SQLite 开 WAL、批量写；分析 BLOB 每曲几百 KB，eMMC/SD 卡友好。
- **交叉编译**：暂缓——先在本机（Linux PC）跑通全部功能；aarch64 产物 / cargo-zigbuild 流程留到功能稳定后再做（纯 Rust 依赖链无 C++ FFI，届时成本低）。
- **feature 裁剪**：symphonia 只开所需编解码器；所有重型可选功能（导入器、MIDI）走 cargo feature，二进制体积可控。

## 3. Workspace 结构

```
/home/windupbird/Workspace/HyperMixx/hypermixx/     ← 新项目（与 mixxx/、mixi/ 并列）
├── Cargo.toml            # workspace，feature 统一开关
├── crates/
│   ├── hypermixx-core/      # 控制总线、beat clock/beatgrid、参数类型、时间换算
│   ├── hypermixx-audio/     # AudioBackend(cpal)、CachingReader、DSP（EQ/FX/limiter）、Keylocker
│   ├── hypermixx-analysis/  # trait Analyzer + BPM/key/波形/静音
│   ├── hypermixx-library/   # rusqlite 迁移 + DAO + 扫描 + Rekordbox/Serato/VDJ 导入
│   ├── hypermixx-midi/      # midir、映射表（TOML）、soft-takeover、LED 反馈
│   ├── hypermixx-ui/        # Slint 界面、Theme/皮肤、波形渲染器
│   └── hypermixx-app/       # bin：装配、线程拓扑、CLI（--skin、--db）
├── benches/              # keylock 引擎基准（M0 spike）
└── scripts/              # 后续：交叉编译、一体机打包
```

依赖方向：`app → 全部`；`ui/audio/analysis/library/midi → core`。core 无 IO/实时依赖。

## 4. 里程碑（按你的要求重排）

### M0 骨架出声 + 定案选型（1~2 周）
- workspace + `hypermixx-core`：ControlBus 最小实现 + 单测
- `hypermixx-audio`：cpal 输出（ALSA/JACK/PipeWire 枚举），48kHz/256 帧，正弦/噪声源；音频线程 FTZ、零分配
- `hypermixx-ui`：Slint 窗口显示 VU + 音量推子（经控制总线）
- **keylock spike（本周必须做）**：用 `yehno - Always.flac`（44.1k→48k），对比 timestretch / signalsmith-stretch / rubato 两级 / bungee-rs（仅对比，GPL 默认排除）：音质、CPU%、内存、可接受 buffer；定 `Keylocker` 默认引擎
- 验收：本机（Linux PC）稳定出声（交叉编译后续再补）

### M1 播放 + EQ + RGB 波形（2~3 周）★你的第一目标
- `CachingReader`：symphonia 解码（FLAC/MP3/AAC/OGG）→ rubato 转 48k → chunk ring
- 播放/暂停/seek/变速（线性变速）+ 基本 cue
- **三段 EQ**（RBJ biquad：low/mid/high，kill 到 +6dB，参数平滑）
- RGB 波形：分析出三带数据，overview + 滚动波形 + 播放头 + zoom

### M2 BPM + key + beatgrid + keylock + beat sync（3~4 周）★你的第二目标
- **BPM 检测**（自研 onset flux + comb filter 第一版；评估 open-bpm 替换）
- **调性检测**（Goertzel 色度图 + Krumhansl-Kessler + Camelot 轮）
- beatgrid 自动对齐 + 手动编辑（offset/BPM 调整 UI）
- **keylock 接入**（M0 定案引擎；异步预处理 + 交叉淡化），附赠 key shift（音高移位）
- **beat sync**：Beat Clock + leader/follower tempo sync + 相位对齐 + quantize + master tempo

> ✅ **M2 已完成（2026-08-13）**。与上面原计划的实测偏差：
> - **BPM/调性/beatgrid 检测复用 timestretch 0.11 自带单遍分析（用户决策）**——
>   不采用"自研 onset flux + Goertzel 色度图"路线；单遍分析产物（BPM/置信度/
>   key + Camelot/offset/拍点表）在波形分析完成后同线程计算（P3），UI 直接消费（P4）。
> - **keylock 引擎 = timestretch 实时引擎**（P1 spike 决策表定案：音高 p95=0.22 cents、
>   CPU p99=0.065、恒定延迟 560 帧@48k；signalsmith 延迟 120ms 出局）。key shift
>   由 rubato 5.0 sinc 级（dsp/pitch.rs）实现，|shift|>3 半音切 WideKeylock profile。
> - **beat sync 已交付**（P5：leader/follower 速率锁定 + set_rate_at 相位校正 +
>   quantize seek）；**master tempo（全局节拍时钟）未实现**，V1 后补。

### M3 FX 全家桶（2 周）★你的第三目标
- filter 扫频（高/低通旋钮）、echo、reverb、flanger、phaser、gate、失真
- FX rack（每 deck 4 槽 + 干湿比）、FX 参数面板、per-channel 参数平滑

> ✅ **M3 已完成（2026-08-14）**。与上面原计划的偏差：
> - **FX 引擎先行、面板等 Flutter**（用户决策）：本轮只交付算法 + `EffectProcessor`
>   trait + 4 槽 FxRack + ControlBus 全驱动接线（56 个总线值）+ 测试；Slint 面板
>   不做，移交 Flutter 迁移阶段，届时经 `fx::manifest` 注册表（8 条 ParamSpec 静态
>   声明）枚举效果/参数生成 UI。
> - **效果 8 个**：echo（2^17 分数延迟 + 反馈阻尼 LP + 拍同步吸附 1/4 拍）、
>   flanger（256 点正弦波表 LFO）、phaser（6 级一阶全通 + 反馈）、reverb（Freeverb
>   public domain 拓扑重新实现，湿声增益由标定测试钉 0.24）、distortion（tanh 驱动
>   + 10ms 增益平滑）、filter-linear（RBJ biquad LP/HP/BP，cutoff log 域平滑）、
>   filter-moog（4 级级联 + 全局反馈，DC 增益/截止衰减/共振峰三测试钉行为）、
>   gate（trance gate：beatgrid 周期 × duty 门控 + 一阶包络去 click，无网格逐位直通）。
> - **FX 挂点**：EQ 之后、gain 之前（引擎与 legacy 两路径同位置）；`load()` 时
>   rack reset。
> - **实测基准**（fx_spike，本机 PC release，329s 全曲逐块）：reverb p99=0.003、
>   4 槽 p99=0.011（验收线 0.30/0.60，余量 >25×）、RTF≤0.008、NaN=0、
>   RSS 增量 ≤0.6MB；torture 臂（moog res 0.8 × echo fb 0.7 × reverb roomsize
>   0.9 最坏参数链）|out|max=9.66 有界（判定线 <20）。engine_sim fx 模式：
>   60s 真曲折磨（100ms 扫 cutoff、2s 翻 enable、3s 扫干湿、5s 换型）播头不停摆。
> - 测试：8 效果单元 + rack 协议单元 + deck 级集成 9 个（新增 7：echo 尾音、
>   gate 跟随 beatgrid、换型写回默认 + click-free、enable 旁路逐位、无网格逐位、
>   seek+EOF 存活、echo 不变调）。

### M4 曲库（3~4 周）
- SQLite 修订版 schema（tracks/track_analysis/cues/playlists/crates/directories）
- 目录扫描（hash 变更检测）+ lofty 元数据 + 浏览/搜索 UI
- 导入：Rekordbox（rekordcrate 评估→备选自研 PDB）、Serato（_Serato_ database V2 + ID3 内嵌 beatgrid/cue）、VirtualDJ（database.xml，需真实样本调研格式）

### M5 MIDI 映射（2~3 周）
- midir 输入线程 → TOML 映射表 → 控制总线；normal/relative/soft-takeover/toggle
- LED/灯输出反馈、设备热插拔

### M6 皮肤与打磨（持续）
- Theme 系统 + 皮肤切换、2→4 deck 布局、快捷键、配置持久化、分析队列管理

### 后续候选（不在 V1）
DVS 时间码（参考 mixi `dvs/`）、麦克风 + talkover ducking、录音、直播、LV2 宿主、Auto-DJ（mixi `automix.rs` 思路）。

## 5. 风险

| 风险 | 缓解 |
|---|---|
| keylock 纯 Rust 质量 vs Rubber Band | M0 spike 用真实测试曲基准实测；trait 保证可换（GPL 引擎留作 GPL 发行选项） |
| timestretch 在 RK3399 的 CPU 占用（大核实时性） | M0 基准含 CPU% 上限；必要时降级为 rubato 两级/预渲染全曲 |
| Slint 许可证与 MIT/Apache 自许可的兼容 | §1.4 路线；发布前二选一 |
| rekordcrate 0.3 成熟度 | 真实 PDB 样本验证；不行用 mixxx kaitai spec 自研（+1~2 周） |
| VirtualDJ 格式无参考 | 需要一份真实 VDJ database.xml 样本（M5 前提供） |
| Slint femtovg 在 Mali-T860/Panfrost 的 GLES 性能 | M0 验证；脏区域更新 + 两级 LOD；降分辨率/软件渲染；skia 最后手段 |
| Pi 音频设备兼容（USB 声卡热插拔） | cpal 设备枚举重扫；M6 与 MIDI 热插拔一起做 |
| FX 在 RK3399 的 CPU（reverb 12 滤波/声道 + echo 2^17 延迟线） | fx_spike 实测余量 >25×（PC）；settled 旁路冻结免 CPU；必要时 reverb 单声道 + width 交叉减半 |
| FX 链极端参数 headroom（moog 共振 × echo 环 × comb 共振） | 各效果有界（单元测试钉死）；torture 实测 9.66× 有界；响度留给用户增益分级/后续 limiter |

## 6. 遗留小问题（不阻塞开工）

1. 一体机的音频接口是 USB 声卡还是 I2S DAC（具体芯片型号）？
2. 板上 Linux 发行版与显示后端？（X11 / Wayland / kmsdrm——影响 UI 后端选择，M0 需确认）
3. 商用路径倾向：GPLv3 发布（最简单）还是保留闭源可能？——不影响 M0~M4 代码，发布前定即可。

---

## 7. Flutter 迁移（2026-08 启动，M6 皮肤替代方案）

Slint 皮肤系统不达标，用户拍板迁移 Flutter（Linux 桌面先行，RK3399 后续）。**Slint 旧 UI（hypermixx-app/hypermixx-ui）保持可编译可运行**，Flutter 为平行路径。

### 7.1 架构

- **flutter_rust_bridge 2.12.0**（两侧钉版，`auto_upgrade_dependency: false`）进程内桥：新 workspace crate `crates/hypermixx-bridge`（cdylib+rlib），静态持有 `ControlBus + EngineHandle + Engine + CpalBackend`（同生命周期）。引擎/分析全在 Rust 线程，Dart 只做渲染与输入。
- **桥 API**（`src/api.rs`，FRB 注解；`src/bridge.rs` 实现 + 7 测试）：`#[frb(sync)] init_engine/ping/snapshot_all/bus_set/bus_get/seek/set_analysis_priority` + `load_track(deck, path, StreamSink<AnalysisEventWire>)`（载曲与分析流合一，消除注册竞态）+ `read_metadata`（lofty 0.25，MIT/Apache 双许可）。
- 载曲/重载：`handle.load` → 清零 grid_bpm/grid_offset 总线 → 停旧分析线程（shutdown 标志 + generation 递增，旧代事件丢弃）→ `start_analysis` → forwarder 线程：TrackAnalysis 写 grid 总线 + 事件经 StreamSink 送 Dart（sink 断开 = forwarder 自退）。
- **60Hz tick**：`EngineController` 单例 `Timer.periodic(16ms)` → 一次 `snapshotAll()`（sync FFI，µs 级）→ 分发到两 deck 的细粒度 `ValueNotifier`（文本只在值变化时更新）。波形 painter 直接读 controller 当前值，`waveTick` 每 tick 递增驱动重绘（widget 不重建）；`waveRev` 仅分析数据变化时递增。每条波形/面板包 `RepaintBoundary`。
- **构建/运行**：`scripts/gen_bridge.sh`（codegen generate + build_runner）、`scripts/run_flutter.sh [曲目]`（cargo build --release -p hypermixx-bridge → 注入 `HYPERMIXX_BRIDGE_LIB` → flutter run；参数 1 = `HYPERMIXX_TRACK` 自动载曲）。`RustLib.init(externalLibrary: ExternalLibrary.open(路径))` 绕开 FRB 默认 loader 的 stem bug（issue #1668）。

### 7.2 渲染决策（D5/D6）

- **滚动波形（两种显示模式共享形状，2026-08-14 拍板）**：连续 f64 窗口数学（列坐标不量化到 1px）→ scatter-max 分数聚合（每列投影到像素区间取 max；`colsPerPx<1` 高放大时线性插值防块状）→ **共享形状** = 每像素列包络 `amp=√(all/255)·halfH` + 三带电平 lo/mi/hi，**孤立尖刺抑制**（单列尖刺高于两侧较高者 1.5× 且差 >8px 压到邻居，治"极细锯齿"闪烁，常量 `_kSpikeRatio/_kSpikeMinPx` 可调）。**两种显示模式仅染色不同**（EngineController.waveMode，settings 落地前经 master 条按钮）：**rgb** = 每列频段归一化混色竖条（复刻 Slint waveform_texture.rs：全频段近白、单频段主导纯色）；**3-bands** = 每列柱内按 low:mid:high 比例切红/绿/蓝三片（中心红、中绿、外蓝，上下镜像），外轮廓与 rgb 完全一致。播放头恒居中（首尾钳制时随动，曲头前留白；**曲尾自然播完回中**——引擎自动停且 playhead 冻结在 duration 附近，`!playing && ph≥dur−0.5` 时 `winStart=ph−winSec/2`）。master zoom `win_dur=60/zoom`，**范围 1~16**（窗口 3.75s..60s，默认 1.0）。beatgrid 竖线 α0.35/0.7，间距 <4px 跳过（moiré 守卫）。点击/拖动 seek：分数数学 + 100ms 节流（seek 重锚 reader + 重置 keylock）。
- **全区半波预览（折叠整流）**：分析 Column 扩 8 字段（每带 p=正峰 / n=负半周 |min| 折叠幅度）；预览每像素双矩形——p 全 α、n α0.55 叠加；三带自底向上堆叠 + beatgrid 线 + 播放头。**播放头由 painter 自绘**（Positioned overlay 在实机渲染异常，见下）。
- **归一化**：每带共享标度 `max(p, n)` → p 字段输出与旧版逐字节一致（Slint 纹理零视觉变化），`progressive_equals_full_sequential` 字节门禁保持。

### 7.3 主屏布局

**P9 三列布局（2026-08-14 重构）**：顶部错误条 → **master 条置顶**（ZOOM 1~16 + 主音量 + master VU + RGB/3-bands 切换）→ 两行**全宽**滚动波形条（deck0 上、deck1 下，各 190px，播放头恒居中）→ 下方三列：左 DeckPanel | **MixerPanel（中心混音台，固定 300px）** | 右 DeckPanel（1px VerticalDivider 分隔）。

**DeckPanel（deck 列）**：行1 = 封面（cacheWidth 降采样）+ title/artist（左）、时间 + key/BPM + tempo 两行（"范围 ±8%" / "当前 ±x.xx%"）+ SYNC/KEY（右，FittedBox scaleDown 防窄窗溢出）；行2 = 全区半波预览（h64，染色随 waveMode）；行3 = **打击垫区**（HOTCUE/LOOP/BEATJUMP/FX 四选项卡与四 pad 列竖向对齐，选项卡行固定 h26 + `tapTargetSize: shrinkWrap`，翻页按钮列与两行 pad 横向对齐）+ **Tempofader 列**（宽 72、高 116：nudge ◀◀/▶▶ 按住键（无 timer，按住语义由引擎 nudge 总线保持）+ 垂直变速推子 ±8%（50% 处 0 刻度线，自绘 track/thumb）+ 微调 −/+ 按住键（`Timer.periodic(100ms)` 每拍 ±0.5%，clamp ±8））；行4 运输行 = **CUE** + 播放/暂停 + FX 架入口（树序保持 DeckPads 之后）+ 载曲（路径输入 + 空则弹 rfd 系统文件对话框）。音量/±8 变速滑杆移出 DeckPanel（→ MixerPanel 垂直推子 / Tempofader）。**播放头恒居中**：`winStart = min(ph − winSec/2, max(0, dur−winSec))`，可为负 → 曲头前透明留白，仅尾端钳制右移；**空条（未载曲）也画居中播放头**（复刻 Slint WaveStrip 无条件画指针）。面板内层 Column 包 `SingleChildScrollView` 防小窗口 RenderFlex overflow。

**MixerPanel（混音台，P9.1/P9.2 布局 2026-08-15）**：两侧旋钮列（各 DECK 标识 + **GAIN → HIGH → MID → LOW → FILTER** 纵向排布，统一 44px）+ 中心音量推子区（**两轨 VU 在推子中间** `[推子0|VU0|VU1|推子1]`，VU 绑 60Hz `dc.vu` 垂直条，配色同 master 条）+ 推子区下方 **XFADE 交叉推子**（无文字，宽度对齐音量推子区不占 EQ 位，中心刻度线 overlay）。**旋钮语义**（MixerKnob）：按住拖拽改值（主导轴：竖直=上推加值、水平=右拉加值，**角度域计值：每侧 150° = 70px、全行程 140px，所有旋钮「拖距 ↔ 角度」比例一致**——EQ 非对称范围同样 70px/侧）、**双击回 0**；**0 值恒指 12 点**（负/正半程各 150° 分段映射，EQ −40..+6 非对称范围 0 仍居中、正向更细）；**单行文字**：平时短标签、拖动中显值、双击后闪值 600ms。EQ 三带绑 60Hz 快照 notifier（唯一事实源，不建本地第二源），gain/filter/xfade 本地状态 + mount 时 `busGet` 初始化（桥缺失 try/catch 回 0）。**双击回正**：音量推子 → 1.0（默认音量，同引擎）、交叉推子 → 0、Tempofader → 0%。

### 7.4 已踩坑（Flutter/FRB 实机）

- FRB `StreamSink` 类型由 `frb_generated_boilerplate!` 生成到 `crate::frb_generated`（不是 `flutter_rust_bridge::StreamSink`）；`sink.add` 需 `T: SseEncode`（默认 SseCodec）。
- enums with fields → freezed sealed classes：freezed/build_runner 进 dev_dependencies、freezed_annotation 进 dependencies；codegen 后必须跑 `dart run build_runner build`。
- Rust edition 2024：`gen` 是保留关键字。
- 生成 Dart：`sync` 字段 → `sync_`；u64 → BigInt（热路径字段用 u32）。
- **Positioned 播放头 overlay（ValueListenableBuilder → Positioned → Stack）在实机（Hyprland/Impeller）渲染异常**（只在与波形重叠处可见、位置漂移），widget test 却正常 → 改 painter 自绘，规避 overlay 层合成问题。
- **FittedBox 子级 Column 必须 `mainAxisSize: min`**：无界约束下 `max` 尺寸退化 → 文本被异常缩放/分带。
- 许可抽查：`cargo tree -p hypermixx-bridge` 112 个 crate 零 GPL/AGPL（MIT/Apache/BSD/Zlib/Unlicense；symphonia MPL-2.0 为既有依赖；allo-isolate 无 Cargo 字段但上游 MIT）。
- 实机验证用 grim 截屏 + 像素分析（模型无视觉时），窗口物理坐标 = hyprctl 逻辑坐标 × 1.2 缩放。
- **pad Stack 非定位子级决定尺寸**：`Stack[Text, Positioned(×)]` 收缩到 Text 大小 → × 压住标签并吸走整块点击；`fit: StackFit.expand` + `Center(Text)` 修复（× 定位到 pad 右上角）。
- **beatjump 显式页表 vs 通用翻页公式**：`_pageCount=(itemCount−8)~/4+1` 对页表 itemCount=2 得 1 页 → 翻页按钮永久禁用；页数按模式分支（beatjump 直接取页表长）。
- **integration_test 引擎在真实线程**：pump 时长不代表引擎时间流逝，所有"等到某状态"用 `DateTime` 截止 + 50ms pump 轮询；keylock 播头瞬态（seek 后 33ms preroll、稳态 0.012s 滞后）→ seek/跳拍断言容差 0.05~0.12s，量化 snap 会落在 0.2s 外的拍点故容差也兼当反量化哨兵。

### 7.5 验收状态（2026-08-14）

- P0 桥回路 ping()=42 上屏 ✓；P1 桥 7 测试绿 + clippy -D warnings ✓；P2 分析 8 字段 + Slint 机械修复（`progressive_equals_full_sequential` 字节门禁绿）✓；P3 主屏 flutter analyze 0 问题 + 3 widget 测试绿 + 真曲实机运行（波形三带渲染、播放头居中、beatgrid、封面/时间/key/bpm 上屏）✓；P4 元数据+控件（封面/title/key/bpm/tempo 双行/sync/keylock/播放/±8 变速/音量/载曲/seek）实机验证 ✓。
- **P4 验收关键修复**：master VU 恒 0 根因 = `EngineController.start()` 只写 `Master.volume` 未写 `DeckN.volume`（ControlBus 控制点初值 0.0 → deck 全静音 → master peak=0）；补写后 widget dump `LinearProgressIndicator(36.2%)` 验证 VU>0 ✓（此前应用一直无声播放）。
- **实机验证法**：grim 物理像素截屏 + `magick rgb:-` + Python 分析（模型无视觉）；**`_flutter.screenshot`（VM service 内部渲染，免疫窗口遮挡）比 grim 可靠**——grim 曾因 wechat 窗口覆盖 hypermixx 误判 VU 为 0。hyprctl clients 先确认窗口几何与 z 序。
- **P6 EQ+FX 面板完成 ✓**：Flutter EQ 三带垂直推子（-40..+6dB）+ FX 4 槽架（bottom sheet，manifest 驱动）。桥 `fx_manifests()` + `DeckSnapshotWire.eq_*`；EQ 推子渲染经 `_flutter.screenshot` 像素验证；**实机 FX 交互经 integration_test 验证**（无系统输入工具，唯一路径）：
  - `HYPERMIXX_BRIDGE_LIB=$(pwd)/../HyperMixx/target/release/libhypermixx_bridge.so flutter test integration_test/fx_panel_test.dart -d linux` → 开面板 → 槽1 选"回声" → 断言总线 `fx1_type=1` + 默认值 `p1..p4=0.375/0.35/0.3/0.0` 写入 + Time/Feedback/Damp/Sync 上屏 ✓
  - 6 widget + 9 bridge tests 绿；`flutter analyze` / `cargo clippy -D warnings` 干净。
  - integration_test 要点：60Hz tick 禁 pumpAndSettle（全显式 pump）；`tester.view.physicalSize=(1600,1800)` 放大视口防 FX 按钮被裁；**tap DropdownButton 本体而非 hint Text**（后者 warnIfMissed）。
- **Slint 布局复刻 ✓（2026-08-14）**：两行全宽波形置顶 + 指针恒居中 + 空条也画指针 + load 按钮弹 rfd 系统对话框。验证：`playhead_center_test.dart` 4 个像素级测试（ph=0 居中+曲头留白 / ph=100 暂停居中 / 尾端 320 右移 x≈680 / 空条居中）+ 共 10 flutter tests 绿 + analyze 零问题。实机 1586×851（逻辑 1586×803 @ DPR2.0）：两 Strip 置顶、deck0/deck1 播放头均 x=959-960 全高居中、面板区封面/EQ 三带/FX、master 条 ZOOM/音量滑杆+VU、无黄色溢出条纹（grim+magick 像素分类确认）。**rfd 对话框需用户手动点击 folder 图标验证**（阻塞 UI，CLI 无法自动化）。
- **波形显示统一 ✓（2026-08-14）**：RGB 与 3-bands 共享同一轮廓（√(all) 包络 + 孤立尖刺抑制去"极细锯齿"闪烁），仅染色不同（rgb=频段归一化混色；3-bands=柱内三带切片 中心红/中绿/外蓝）。master zoom 范围 1~16（窗口 3.75s..60s）。曲尾自然播完指针回中。验证：`wave_mode_test.dart`（共享轮廓逐列顶行一致 / 尖刺抑制 / RGB 归一化颜色 / 两模式渲染不同 / 3-bands 切片 / overview 越界）+ `playhead_center_test.dart` 新增曲尾回中 x=400 —— 共 17 flutter tests 绿 + analyze 零问题。实机交互由用户自测（RGB/3-bands 切换、zoom 两端、播完回中）。待续：M1 听感确认、master tempo、Flutter 后续里程碑（曲库/MIDI/pitch/手感确认）、RK3399 验证。
- **P8 CUE/打击垫完成 ✓（2026-08-14）**：主 CUE 按钮（play 旁）+ 2×4 打击垫区（deckinfo 下方，HOTCUE/LOOP/BEATJUMP/FX 四选项卡与四列对齐 + 右翻页按钮，窗口 8、步长 4）。
  - **CUE/hotcue 状态机**（`cue_action.dart` 纯函数，ε=0.05s）：播放 → 召回（seek_exact 不量化）；停播远离 → 落点；停播位于点位 → 按住试听（play=1）、松开停播回点。hotcue 16 槽（3 页），右键或角上 × 删除；载曲 reset 回曲首。
  - **LOOP**：1/32..64 拍 12 项（2 页），点击激活/再点取消。引擎 `set_beat_loop(beats)`：loop_in=grid.snap(pos) 量化、loop_out 钳制到曲长；块首 `playing && pos≥loop_out → seek(loop_in)` 回跳；外部 seek/beatjump 出环自动取消；载曲复位。
  - **BEATJUMP**：显式页表（p0=左右 1/2/4/8，p1=左右 16/32，空 4 pad），简单加减、拍长匹配当前速度（含 nudge/sync）。
  - **FX**：8 pad = 8 槽（rack 4→8，与引擎同步扩），点击开/关 enable，右键开面板定位该槽。**P8 修复**：换型不再强制 enable=1（旧行为使 FX 关不掉），enable 只跟 ON 开关。
  - **引擎/桥**：deck.rs 拆分 `seek_exact`（cue/hotcue 召回不量化）/`seek_seconds`（quantize 量化 + 出环取消）/`seek_internal`（epoch++/清缓存/keylock 重锚）；桥 `DeckSnapshotWire` 增 loop_active/loop_in/loop_out + `seek_exact`/`beatjump`/`set_beat_loop`。
  - **验证**：Rust 115 audio + 8 core + 11 bridge 测试绿（fx 换型 enable 回归、loop 回跳 wraps≥3、beatjump 速率匹配、出环取消、桥 snapshot loop 字段）；Flutter 32 widget 测试绿（`deck_pads_test` 10：FakeActions 注入验证四模式语义/翻页/删除/CueButton；`cue_action_test` 5：状态机边界；fx_panel 8 槽）+ **2 个 integration_test 真引擎全链路绿**（`deck_pads_test`：正弦 WAV 载入 → grid 120 写死 → CUE 落点/试听/召回 → LOOP 0.5s 激活取消 → BEATJUMP +2.0s → FX enable 开关/右键面板 → HOTCUE 落点/删除；`fx_panel_test` 8 槽）。**载曲后引擎自动开播（deck.load() → playing=true）**——测试须先显式停播再验 CUE 落点语义；等待条件以 dc（60Hz tick 分发）为准而非总线（控件读 dc，总线领先 ≤16ms）；两次点按之间留 100ms 等 tick 重建（pad enable 闭包读旧 build 态则关不掉）。实机手感由用户自测。
- **P9 混音台 + 布局重构完成 ✓（2026-08-14）**：主屏改三列（中心 MixerPanel 混音台、master 条置顶）；引擎新增三条总线：`DeckN.gain`（dB，−12..+12，默认 0 → `10^0=1.0` 精确恒等）、`DeckN.filter`（−1..+1，正=LP 20kHz→20Hz、负=HP 20Hz→20kHz、0=旁路）、`Master.crossfader`（−1..+1，最简曲线：居中 (1,1) 精确恒等、向一侧线性衰减另一侧）。
  - **DeckFilter 新模块**（`dsp/deck_filter.rs`，用户确认引擎新做 DSP 非 FX 复用）：**三路混合** `y=dry·x+ml·LP(x)+mh·HP(x)`（ml=max(knob,0)、mh=max(−knob,0)、dry=1−ml−mh）——两臂权重走 10ms 一阶 Smoother、永不瞬时换臂，MIDI 绝对 CC 跳变/过零翻转无 click（实测单 biquad 换型与双 biquad 交叉淡化在"臂交换"瞬时都有 |lp−hp| 量级阶跃，弃用）；截止频率 log2 域逐采样平滑（对数轴听感均匀）、>0.02 倍频程才重算系数；off→on 沿清四臂陈旧 z1/z2（首样本 = b0·x 且干声权重 ≈1）。
  - **bitwise 直通三条件**：滤波旋钮 0 且全 smoother 稳定 → 整模块跳过；gain `10^0==1.0`；crossfader 中心因子 (1,1) —— 既有 deck bitwise 测试即回归防线。
  - **DSP 链**：keylock → pitch → EQ → **deck 滤波** → FX rack（8 槽）→ gain（音量 × 通道增益 dB）；load() 重置滤波、**不重置 gain_db**（trim 是调音台设定，跨曲保持，区别于 volume）。
  - **验证**：Rust 130 audio + 10 core 测试绿（deck_filter 7：bitwise 直通 / LP·HP 全开杀频 / 半开偏频 / 切入切出与过零翻转 clickfree（逐采样 Δ<0.2）/ 全扫无 NaN；deck 4 新：gain 恒等 / ±12dB≈×3.98·×0.25 / 滤波直通 / 滤波杀高；engine crossfade_factors 表驱动 4）+ clippy -D warnings 干净 + 桥重建；**Flutter 46 widget 测试绿**（新增 mixer_knob 5：双轴拖拽主导轴计值 / clamp / 双击回 0 / initFromBus 无桥回 0 / 标签渲染；mixer_panel 3：两通道 2×5 旋钮 + 3 推子结构 / 无桥初始化；tempo_fader 4：nudge 按住 ±1 松开 0 / 微调 100ms 重复 / 推子拖拽计值 / 渲染；overview_style 3：切 waveMode 同像素不同 / beatgrid 移除后原白列透明 / 播放头仍在）+ **2 个 integration_test 真引擎绿**（fx_panel 'FX'×4 + 树序 `.last` 保持、deck_pads 全链路）。overview 预览删除 beatgrid、染色跟随 waveMode（rgb = 单混色列 p 全 α/n α0.55 折叠，bands = 三带堆叠保留）。**坑**：test 手势吃 ~20px slop（拖拽断言用超满量程位移或放宽容差）；MixerKnob 无 onTap（双击判定延迟 300ms）→ 拖拽后双击识别器残留 40ms 计时器需 pump 到期；EQ 三旋钮并排 128px 通道条内用 size=40。旋钮/推子手感与实机布局由用户自测。
- **P9.1 混音台布局调整完成 ✓（2026-08-15）**：用户细化——音量推子居中、两轨 VU 夹推子、旋钮两侧纵向 gain→high→mid→low→filter（统一 44）、0 恒指 12 点、单行文字（平时标签 / 拖动显值 / 双击闪值 600ms）、XFADE 对齐推子区且无文字、所有推子含 Tempofader 双击回正（交叉→0、tempo→0%；音量双击目标 P9.2 改为 1.0）。
  - **验证**：Flutter 49 widget 测试绿（mixer_knob 5 重写：拖动后回标签 / 拖动中显值 / 双击闪值后回标签 / initFromBus 小拖 |v|<3dB 证初值 0；mixer_panel 5：旋钮纵向顺序 dy 断言 / VU 绑 dc.vu 0.7·0.0 / 无 XFADE 文字 / 无桥初始化；tempo_fader 5：双击回正 0%）+ analyze 零问题 + 2 个 integration_test 真引擎绿（deck_pads 首跑遇既有 transient 启动失败，单独重跑绿）。音量/交叉推子双击回正走引擎总线、widget 测试无桥不可测——实机验证归用户。
  - **坑**：`startGesture + moveBy` 单段大位移只触发 pan 接受、不递增量 → 分段小步拖；所有手势测试末尾 pump ≥700ms（双击识别器 40ms + 闪值 600ms timer）；VU 测试直接改 `dc.vu.value` + addTearDown 复位（EngineController 跨测试单例）；Slider 包 onDoubleTap 会使单击跳值延迟 300ms（双击识别器 arena 持留，拖动不受影响）——用户要求双击回正，接受该代价。
- **P9.2 混音台手感微调完成 ✓（2026-08-15）**：用户三项——① 音量推子默认 **1.0**（`DeckController.volume` 初值 0.75→1.0，与引擎默认一致；双击回正目标同步 1.0）；② 旋钮「拖动距离 ↔ 旋钮角度」比例全局一致：MixerKnob 改**角度域拖动**（Δ角度 = 像素 × 150°/70px，再 `knobValue` 逆映射回值），删 `pixelsForRange` 参数（原 EQ 180 与其余 140 不一致，且 EQ 正/负侧 °/单位差 6.7 倍，现在每个旋钮每侧恒 70px）；③ 两轨 VU 移到两推子中间 `[推子0|VU0|VU1|推子1]`。验证：49 widget 测试绿（mixer_panel VU 用例加横向顺序断言推子0<VU0<VU1<推子1；mixer_knob 既有拖拽数值原样成立——对称旋钮 70px/侧与原 140px 全行程等价）+ analyze 零问题 + 2 integration 绿。
- **P10 拍同步重做 + loop/beatjump 量化 + 网格检测深改完成 ✓（2026-08-15）**：用户三个问题（sync 无法对齐节拍 / loop·beatjump 后不对齐 / 网格线基本不正确）全面整改，用户拍板：同步推子松手回弹、Loop 最近拍立即生效、Beatjump 立即跳跃整拍量化落点 snap、网格检测深改。
  - **P10.1 引擎连续 PI 同步（deck.rs）**：`apply_sync` 重写为每块 PI 控制——follower 速率 = tempo 比 ×（leader 有效 BPM / 本曲 BPM）×（1 + Kp·相位误差 + Ki·积分），死区 0.02 拍、修正 ±0.2 上限、积分抗饱和；**每块值变化即重发有效引擎速率**（修复深根因：旧实现从不发基础速率，leader 拉推子后 follower 速率永远停在旧值）；含 pitch 轴 bug 修复；seek 后自然重收敛。**beatjump 量化**：拍长用网格 BPM（去 rate 因子），落点无条件 snap 到网格；两轨同速时相对相位恒不变。**Flutter 接线**：quantize 总线默认开、tempo 推子同步期输入锁定（松手回弹）、tempo 显示有效速率（effRate = bpm/gridBpm−1，bpm 字段保持静态网格 BPM 供 loop pad 用）。
  - **P10.3 loop 无 reset 回绕（deck.rs）**：deck 侧 loop 缓冲状态机（Idle/Capturing/FromBuffer）——捕获落在 [loop_in, loop_out) 的帧、切环按 feed 位置判定、从缓冲循环喂 keylocker（重锚无 reset、无 epoch 递增无欠载）、显示播头折返、退出从 ring 头（停在 loop_out）无缝续播；缓冲上限 min(64 拍, 30s) 超限回退 reset 路径；seek/beatjump 落入清缓冲。接缝零 click 由 graph.rs 既有测试背书（0.92/1.0/1.08 比率跨 wrap 续喂零 click）。
  - **P10.2 网格检测深改（timestretch-rs + hypermixx-analysis + bridge，PREANALYSIS v11）**：双分辨率检测——12k 粗链（经典 ODF 链）定 tempo 层级，48k 细链（**superflux ODF**：32 log 带 25Hz–Nyquist、log 压缩、**因果 max-pool**（对称池化实测 704 样本前导偏差，已修）、superflux 0.55/phase 0.25/energy 0.20 三路混合）DP 定拍位；**resolve_octave** 八度硬判定（hint ±15% 命中即赢，近并列取近先验 log-BPM 距离；先验 σ 1.0→0.6）；**rigid 拟合加深**：BPM ±2%/401 步、相位 512 步（≈1ms@120）、kick 包络 48k 重算、八度守卫、beats 补齐曲首（offset 语义 = 网格锚点）。**snap 按路径参数化**（实测发现：经典链 DP 拍系统性晚 29ms = 一整个检测延迟，3 帧 snap 是对齐机制；细链因果池化无此偏移，1 帧防误抓邻拍）。**集成**：mono 48k 直通累积（(L+R)/2，16s/段 ≈3MB）；track_analysis 用 detect_beats_multi + 48k rigid 细化；低置信 <0.25 → bpm=0 且 bridge 不写 grid 总线（保留旧网格，防引擎 sync/loop 停摆），beats 仍转发画网格线。**验证**：timestretch CI 五项全绿（lib 606 + qa 46 等全 target、clippy -D warnings、fmt、rustdoc）；hypermixx 全 workspace 测试绿（segment 收紧至 ±0.3 BPM / ±10ms、bridge 低置信门槛新用例）+ clippy + bridge 重建 + flutter analyze/50 widget 测试/2 integration 全绿。QA 自测基线一致（经典路径行为保持）。**实机听感归用户自测**（约定）：grid 视觉、sync 手感、实曲 BPM 精度 A/B（corpus 音频不在本机，下载被网络阻断）。
- **P11 收尾圈 loop 修复 + 有效 BPM 显示 + loop/cue 波形提示 + 滚动波形重构完成 ✓（2026-08-15）**：用户四项——① deckinfo BPM 显示变速后数值；② loop 释放后从当前位置继续（旧 bug 双重跳变：音频跳 loop_out、显示跳 N×环长）；③ loop/cue 波形提示（滚动+预览）；④ 滚动波形重构（播完指针不再后移/回中、sync 后两轨滚动速度一致、低频上限拉低到纯鼓点、beatgrid 加粗）。
  - **P11.1 引擎 loop 收尾圈 + 播头重锚（deck.rs）**：根因搞清——timestretch-rs `source_position()` 是**出引擎音频的位置标签**（= set_track_position 锚点 + 顺序推入累计，每块 process 末尾发布）；FromBuffer 环绕每圈喂 len 帧标签累计 k×len，退出后旧公式 `pos = feed_base + sp` 暴露累计 → 显示跳 N×环长。修复：释放时 FromBuffer **不立即退出**，喂完当前圈后游标圈界切回 ring（feed_pos/feed_chunk 停在 loop_out，音频从 loop_out 无缝续进）；**新锚点 `pos_base = loop_in×sr + ((sp − loop_sp_anchor).rem_euclid(len)) − sp`**（= −k×len，吸收标签累计；pos_base 可为负故独立 f64 字段，**切环时不 set_track_position 重打标签**——重打会造标签断层显示反跳）；播头公式三分支 FromBuffer 折返 / pos_base+sp / feed_base+sp；load/seek_internal/rebuild_keylocker 三处清 pos_base。seek/beatjump 出环仍走 reset 既有路径。
  - **P11.2 deckinfo 有效 BPM（deck_controller.dart）**：新增 `gridBpm`/`effBpm` notifier（dispose 同步释放）；`effBpm = s.bpm`（引擎每块写 grid×rate，无网格回退静态值），bpmKeyText 改读 effBpm；`bpm.value` 保持静态语义（deck_pads 拍数匹配依赖）。
  - **P11.3 loop/cue 波形提示**：滚动波形 `_paintOverlay` loop 区域绿填充 `0xFF2E7D32` α0.12 + 边界 2px `0xFF66BB6A` α0.9、cue 橙竖线 `0xFFFF7043` + 顶部横帽、hotcue 深橙 `0xFFE65100`（空槽不画）；overview 同款按 durSec 比例（1.5px 线）；两 painter repaint 合并扩入 loopActive/loopIn/loopOut/cuePoint/hotcues（停播时 playhead 不动，不加不重绘）。
  - **P11.4 滚动波形重构**：**4a 播完停右缘**——删 ended 回中分支（尾钳制 min(ph−winSec/2, dur−winSec) 已使 ph=dur 时指针在右缘），wave_strip `_seekAt` 同步删；**4b 拍轴统一横向坐标**——顶层纯函数 `waveWindowFor(ph, dur, winSec, gridBpm, effBpm)`：网格可用时 `winSecTrack = winSec×effBpm/gridBpm`（= winSec×rate），同步两轨 effBpm 相等 → 窗口拍宽相等、滚动速度 w/winSec 与 rate 无关、同一乐拍同 x、grid 线跨轨对齐；无网格回退秒轴（rate=1 逐像素不变）；**4c 低频上限 300→150Hz**（waveform.rs LOW_HZ，红色只表征鼓点）；**4d grid 线 1px→2px、下拍 3px**（moiré 守卫不变）。
  - **验证**：Rust 143 audio + 11 bridge 测试绿（`loop_exit_resumes_linear_without_gap` 收紧：释放后跑 2s 播头 = 释放位置+2.0 ±0.02；新增 `loop_exit_no_per_block_jump` 逐块 Δ∈[0, 256/48000+0.01] 钉死无跳变、`loop_reactivate_after_release_lands_on_quantized_pos` 释放相位窗口 [0.25,0.3) 保证二次捕获完整 → 落点精确 1.5 不 reset；`loop_redeactivate_reuse_buffer` 断言改收尾圈语义 FromBuffer）+ clippy -D warnings + bridge release 重建；Flutter 59 widget 测试绿（新增 `wave_window_test` 4：秒轴回退/停右缘/同步两轨同乐拍同 x 数学精确/滚动速度 w/winSec 与 rate 无关；`wave_markers_test` 4 像素级：滚动+overview loop 绿边界/填充、cue/hotcue 橙线、空槽无像素、不遮播放头；`playhead_center_test` 曲尾例改断言停右缘 x≈799）+ analyze 零问题 + 2 个 integration_test 真引擎绿（单独依次跑）。**坑**：marker 像素断言须选整像素 x（非整数坐标部分覆盖稀释颜色）与无波形内容行（overview 折叠负半最高到 y≈6）；bridge 测试首轮全量并行跑出现过一次瞬时失败，单独重跑 3 次 + 全量重跑 2 次不复现（判定首次并行 IO 竞争一次性抖动）。**实机听感归用户自测**：loop 收尾圈、sync 滚动一致性、鼓点红色纯度、150Hz 低频视觉、grid 线粗细。
- **P12 beatjump 延迟/同步丢失 + 滚动波形到结束时仍右移修复完成 ✓（2026-08-15）**：用户两项——① beatjump 点击后有延迟导致失去同步；② 滚动波形到结束时仍右移（P11.4a 修复后复现）。
  - **bug 1 修复（beatjump 同步丢失）**：**UI 延迟根因** = `onTap` 等手势仲裁（抬指 + 竞争判定 ~数十毫秒）→ beatjump pad 改 **onTapDown 按下即跳**（deck_pads.dart）。**引擎侧**：`EngineOp::BeatJump` 联动跳（engine.rs）——sync 任一开且另一轨在播 → 两轨同拍数一起跳（各按各 grid 落点，相位都归整拍）→ 相对相位保持、跳后无 PI 慢收敛 flam；两轨 seek 重建缺口同时发生，听感是短暂共同停顿而非错拍。
  - **bug 2 修复（播完右移）**：引擎探针 + 集成测试双重实证：真实引擎播完 pos 冻结在 dur−10ms 静止（EOF 判停 ✓）。**真根因在 sync 场景**：leader 播完停 → follower apply_sync 首 guard（`!leader.playing`）提前返回，而 update_params 每块把 rate 重写回滑杆值 → effBpm（=grid×rate）跳变（如 124→120 跳 4bpm）→ 拍轴窗口 winSecTrack=winSec×effBpm/gridBpm 瞬变 → winStart 平移 ≈26px"到结束时右移"。修复：**P12 速率连续**——sync 开启时 update_params 不再重写 rate（保留上块 sync 速率）；**滑杆接管补丁**——新字段 `last_slider_rate`/`slider_takeover`：滑杆变化 = 接管意图（sync 失效时恢复滑杆调速、直发引擎），apply_sync 活跃覆写后清标志（同步优先），无网格 fallback 分支保持每块写滑杆值。
  - **测试 rig 深挖（beatjump 落点 2.467 谜团）**：engine 级测试初始失败根因 = **warm_start preroll 是 1584 帧（latency 560 + settle 1024）不是 560**——seek 后 feed_base = target−1584，测试 rig 无 reader 线程、seek_internal 清空 chunk ring 后无人补喂 → 欠载、sp 冻结 0 → 播头 = feed_base = 2.467 精确吻合。既有 deck 级 beatjump 测试（1e-6 容差）不跑 process() 读的是 seek_internal 直设的 pos 故能过——**真实路径（每块 process 重写 pos = feed_base + sp）只有 engine 级测试覆盖**。修复：夹具新增 `test_refill_after_seek`（补推 64 chunk 模拟 reader 响应 Seek）+ 断言改真实引擎语义——落点 = snap 目标 − 引擎延迟补偿（≈560 帧）+ 推进（容差 0.03），核心断言**两轨跳后相对相位一致 <0.005 拍**（bug 1 的目标：无 flam）。
  - **验证**：Rust 147 audio + 10 core + 11 bridge 测试绿（engine 级 3 新测试：`beatjump_with_sync_links_both_decks`（联动 + 相位一致）、`beatjump_without_sync_jumps_only_target`（不联动 + 另一轨正常推进）、`sync_follower_rate_holds_after_leader_stops`（leader 停后 bpm 连续不跳回滑杆 + 无网格 fallback 滑杆 +4% 生效）；deck 探针转正式回归测试 `eof_playhead_freezes_at_track_end`（EOF 后 64 块播头逐块冻结）+ 全 workspace 测试 + clippy -D warnings + bridge release 重建；Flutter analyze 零问题 + 59 widget 测试绿；**3 个 integration_test 真引擎依次单独跑全绿**（deck_pads / fx_panel / eof_playhead：frozen_ph=11.9927 ≤ dur+0.05、停止后 1.5s 采样冻结）。**实机听感归用户自测**：beatjump 手感（按下即跳 + 联动停顿感）、sync 中跳拍后相位、播完滚动波形停右缘。
- **P13 波形预览/滚动波形分离（固定居中 + 已播蒙层 + 外推顺滑）完成 ✓（2026-08-15）**：用户要求"波形预览和滚动波形分开，预览是固定波形+进度条，滚动波形是固定进度条和滚动波形，注意性能和流动顺畅"。设计经选项确认：滚动条**播放线固定居中**（Serato/Traktor 式，删 P11.4a 尾端停右缘）；预览条**竖线 + 已播蒙层**（已播 ▓ 深、未播 ░ 浅）。
  - **滚动波形固定居中（scrolling_wave_painter.dart）**：`waveWindowFor` 删尾端钳制（`min(ph−winSecTrack/2, max(0,dur−winSecTrack))` → `winStart = ph − winSecTrack/2` 首尾不钳制；dur 参数移除，新签名 `(ph, winSec, gridBpm, effBpm)`）——曲头前负值留白保留；曲尾播到底窗口越出曲长、越界列读 0 → 右半留白、线恒在 w/2。wave_strip `_seekAt` 同款公式自动一致（点击映射 = 显示）。
  - **预览已播蒙层（overview_painter.dart）**：durSec>0 时全区蒙层——已播 `黑 α0.38`、未播 `白 α0.10`，边界 = displayPlayhead/durSec×w；蒙层画在 loop/cue 标记与播放头之下不遮标记。
  - **displayPlayhead 外推（deck_controller.dart，性能/流动顺畅核心）**：60Hz tick 采样 5.33ms 块步进的 playhead → 高放大（3.75s 窗）直接采样每帧 2-4px 阶梯跳变。新增独立方法 `updatePhSample(ph, playing, bpm, gridBpm)`（不经桥、可单测）：playhead 推进 → 重锚 + 按有效速率 bpm/gridBpm 外推（无网格 1.0），paint 时 `displayPlayhead = 采样 + 速率×Δt` 补满 tick 间隙滚动连续；停播/欠载（采样连续不变）→ 速率冻结 0 钉在最后采样点、不越过引擎真值；seek 跳变重锚直接跟跳不跨 seek 插值；无采样（纯 widget 测试直设 playhead）回退真值。滚动窗口、overview 蒙层边界/播放头、wave_strip 点击映射三处统一改用 displayPlayhead。**评估后未做**：滚动条复用 fullOverview 粗列（默认 zoom ≈28 列/px 扫描仅 ~180K 字节读/帧收益边缘、引入第二数据源风险；瓶颈在每帧 800 矩形 canvas 绘制不受影响）。
  - **验证**：Flutter analyze 零问题 + 65 widget 测试绿（`wave_window_test` 改签名去 dur、曲尾例 299/比例 0.5 固定居中替换旧 269/1.0 停右缘；`playhead_center_test` 曲尾两例改断言 x=400 恒居中 + 右段越界透明留白；`overview_style_test` beats 移除例透明 → 未播浅蒙层 α18-40 中性色；`wave_markers_test` overview 区间外透明 → 浅蒙层；新增 `display_playhead_test` 6：无采样回退 / 推进外推 / 同步变速外推 bpm/gridBpm / 停播冻结 / 欠载冻结 / seek 重锚）+ 3 个 integration_test 真引擎依次单独跑全绿（引擎零改动，无需重建桥）。**实机听感归用户自测**：滚动连续感（外推）、曲尾留白观感、蒙层深浅配色、高 zoom 平滑度。
- **P14 beatjump 消卡顿 + 去联动跳 + sync 一次性对齐/速率锁 + 解锁不改播放完成 ✓（2026-08-15）**：用户三项——① beatjump 仍然卡顿（P12 未解决）；② sync 后两边会一起 beatjump，应仅简单跳拍（跳拍时长 = 60/bpm×beats 已确认 = grid.period_secs()）；③ sync 目标逻辑：两侧 BPM 保持一致（速率锁）、节拍仅在开启时快速对齐（旧连续 PI 每块追相位导致 sync 下 seek/微调进度被拉回失败）、取消 sync 时速度推子仅解除锁定不改变播放状态（旧每块 rate=滑杆 → 关 sync 瞬跳）。
  - **P14.1 beatjump 消卡顿（deck.rs seek_internal）**：根因 = timestretch `warm_start(preroll)` priming 期间输出被丢弃——全预卷 1584 帧（latency 560 + settle 1024）= **每次跳 33ms 静音**（P12 联动跳双轨双倍）。修复：`seek_internal(seconds, min_preroll)` 增参——beatjump 走 **warm_start(1)**（done_at = max(0, 1−560) = 0 → priming 立即完成 + 64 帧 declick 淡入 ≈0 静音，先例：deck rebuild 路径 warm_start(1)）；其余 6 个 seek 调用点全预卷不变。显示语义与常规 seek 稳态一致（pos = 落点 − 560 帧延迟补偿 + 推进）。
  - **P14.2 beatjump 去联动（engine.rs）**：删 P12 联动跳（sync 后两轨一起跳不符合预期）——仅跳目标轨，各自独立落 grid 拍；sync 速率锁保持 BPM 一致，跳后无相位追踪不 flam。
  - **P14.3 sync 一次性对齐 + 速率锁（deck.rs apply_sync 重写，删 SYNC_KP/KI/I_SAT 积分器）**：update_params 边沿检测——sync 开启沿置 `sync_align_done=false`；follower 未对齐时相位误差 wrap±0.5 拍，`|err|<0.01 拍` 死区 → 锁定；否则 rate = target×(1+corr)、corr = err/1.0 拍 clamp ±0.5、rate clamp [0.5,2.0]（约 1.6s 最坏收敛）；**锁定后每块 rate = target**（= leader.grid_bpm×leader.tempo_rate/follower.grid_bpm，grid 比精确补偿 → 变速立即跟随）且**不再追相位** → sync 下 seek/微调保持新相位不被拉回。
  - **P14.4 解锁不改播放（sync_held 锁存）**：关 sync 沿记录滑杆位置、置 `sync_held=true`（**不重写 rate**——播放速率保持 sync 期间值，无瞬跳）；滑杆实际变化才接管（rate=滑杆、清锁存）；sync 开启沿/load() 清锁存。滑杆接管补丁（P12 last_slider_rate/slider_takeover）与锁存合并成统一滑杆语义状态机。
  - **Flutter tempo 推子（tempo_fader.dart）**：thumb 恒显 `dc.effRate`（实际速率：sync 中锁 leader、关 sync 保持 sync 值**不跳回滑杆位置**，删 P10.1 松手回弹语义）；微调基准改 `effRate`（关 sync 后 bus 值与实际速率不一致，读 bus 会瞬跳）；同步期间拖拽锁定不变。
  - **验证**：Rust 全 workspace 184 测试绿（audio 149：新增 `sync_disengage_keeps_rate_until_slider_moves`（关 sync 后 bpm 连续不跳回滑杆、播头继续推进、滑杆移动后接管生效）、`sync_seek_after_align_not_pulled_back`（对齐后 seek +0.37 拍、4s 后相位不变不被拉回）；engine 级 2 测试改写——`beatjump_with_sync_jumps_only_target_deck` 跳 deck0 后 deck1 按 sync 速率锁 120/124 推进不联动、落点断言 = 落点 − 560 帧 + 推进（最小预卷无 priming 丢弃偏移）；既有 9 个 sync 测试对齐收敛窗口内全过）+ clippy -D warnings + bridge release 重建；Flutter analyze 零问题 + 65 widget 测试绿（tempo_fader 测试改题：同步拖拽锁定 + thumb 恒显 effRate、滑杆 bus 变化不重绘、关 sync 拖拽恢复）；3 个 integration_test 真引擎依次单独跑全绿。**实机听感归用户自测**：beatjump 卡顿改善（33ms→≈0）、sync 开启对齐速度/关 sync 保持手感、跳拍落点。

- **P15 推子软接管（回位生效）+ sync 暂时加减速（波形不缩放）完成 ✓（2026-08-15）**：用户两项——① 取消 sync 后推子位置可能与当前速率脱开（"推子不在当前速度下"），变速需推子回位后生效；② sync 时也能暂时加减速，操作后不再自动对拍，暂时加减速时波形不缩放。
  - **P15.1 非 sync 软接管（fader_detached）**：关 sync 沿按 `|滑杆 − 当前速率| > 0.5 百分点` 置位（rate 保持 sync 值不变，P14.4 延续）；置位后滑杆移动**需先回位**（进入当前速率 ±0.5 百分点带）才恢复直通——带外移动（含触摸跳变）不生效，带内落点 = 接管。P14.4 的 sync_held 改名 fader_detached。
  - **P15.2 sync 期间推子软接管（fader_armed）**：sync 中推子小步穿过目标速率带（`FADER_TAKEOVER_EPS=0.5` 百分点）翻转 fader_armed（带内离开 / 带外符号翻转；**大步 > FADER_STEP_MAX=3.0 百分点/块 = 触摸跳变不翻转**）；armed → rate = 推子（暂时加减速），回到带内重新锁定 rate = target。接管/锁定均置位 sync_align_done → **操作后不再自动对拍**（只在 sync 开启沿对齐）。穿过判定基准 `fader_prev_rate` 存上一块 apply_sync 的推子位置（块尾更新；放 update_params 会令 step 恒 0 永不翻转——开发中实测发现的单位/时序两个 bug：EPS/STEP_MAX 是百分点需 ×100 比较、prev 更新点需在 apply_sync 块尾）。
  - **P15.3 nudge 移引擎轴（engine_rate）**：nudge 倍率 ×1.08/÷1.08 从 slider_rate/self.rate **移至 engine_rate**（keylock 开时音高不变性保持：engine_rate = rate/2^(shift/12)×nudge）——sync 速率锁不再覆盖对拍 nudge（旧实现 sync 下 nudge 无效），**sync 时按住 nudge = 暂时加减速**。显示 BPM = grid×rate 不含 nudge；apply_sync 写 bpm = grid×**target**（锁值而非 armed 偏离值）→ **暂时加减速时 BPM 显示/波形窗口（P11.4b winSecTrack = winSec×effBpm/gridBpm）不缩放**。
  - **Flutter tempo 推子**：sync 期间拖拽照常写 bus（生效与否由引擎软接管判定，删 P14 的 sync 拖拽锁定）；微调 sync 期间不写（直接写会瞬移推子位置；暂时加减速用 nudge/推子）；thumb 仍恒显 effRate。
  - **验证**：Rust 全 workspace 186 测试绿（audio 151：改写 `sync_disengage_soft_takeover_until_fader_returns`（关 sync 后跳 +4% 不生效、0% 回位接管、再 +4% 直通 124.8）；新增 `sync_fader_temporary_takeover_then_relock_without_realign`（小步拖过 0% 带 → armed 0.92× 播头减速、BPM 锁 120、相位差积累；拖回穿带重新锁定 1.0×、相位差不追回）、`sync_nudge_bends_tempo_without_bpm_rescale`（sync 下 nudge 2s ×1.08 播头加速、BPM 锁 120、相位差积累；松开恢复不追回）；既有 `sync_ignores_follower_slider` 由跳变守卫保护、9 个 sync 测试全过）+ clippy -D warnings + bridge release 重建；Flutter analyze 零问题 + 65 widget 测试绿（tempo_fader 测试改题：sync 拖拽写 bus + 微调 gate + thumb 恒显 effRate）；3 个 integration_test 依次单独跑全绿。**实机听感归用户自测**：回位手感（EPS 带宽容度）、sync 中推子暂时加减速与 nudge 弯速听感、波形不缩放的观感。

- **P16 beatjump 整拍跳距（去落点 snap）+ deckinfo sync/key 竖排 + leader 指示完成 ✓（2026-08-15）**：用户两项——① beatjump 仍然没有整拍跳跃；② deckinfo 右侧 sync/key 竖直排列对齐、增加 leader（master deck）指示（特殊颜色）。
  - **P16.1 beatjump 跳距精确整拍（deck.rs beatjump 去 snap）**：根因 = 旧 `grid.snap(pos + N×period)` 在当前位置不在拍上时引入 ±0.5 拍就近误差——跳距 = N±(0.5−相位) 非整（例：1.3s 处按 ▶3 实际跳 3.4 拍）。P14 用户已确认"跳跃时长 = 60/bpm×beats"即跳距整拍；P10 的"量化"亦本意此。修复：`target = pos + N×period`（不吸附）——在拍上按时落点天然在拍线（sync 锁定相位≈0 恒拍线，两轨对齐保持）；离拍按时跳距精确整拍。测试改写：`beatjump_source_beat_domain_exact_distance`（两次跳距均 = 2s 源拍、rate=2 不变）、`beatjump_exact_distance_and_noop_without_grid`（1.3+1 拍 = 1.8 不再 snap 2.0）、`beatjump_exact_distance_at_any_rate_sync_off`（三速率跳距 = 3 拍整）；engine 级 `beatjump_with/without_sync_jumps_only_target_deck` 的 want0 去 snap（`p0 + 4×60/120`）。
  - **P16.2 deckinfo sync/key 竖排对齐（deck_panel.dart）**：SYNC/KEY 两 toggle 从行内移出，右侧 Column 竖排（间距 4、同宽 48 水平居中对齐）；`_toggle` minimumSize 固定 48×28。
  - **P16.3 leader（master deck）指示（纯 UI，零 FRB/wire 改动）**：`_isSyncLeader` 判定与引擎 beat sync 规则逐字一致——单开 = 不开 sync 的轨、双开 = deck0、都关无；leader 轨 SYNC 按钮加 amber 边框 + 字色（0xFFFFB300，leader 恒 sync 关，与"开"蓝底不冲突）。重绘：`_infoColumn` 外层 `ListenableBuilder` 监听**两轨** syncOn（否则另一轨开关 sync 后本面板指示不刷新——开发中实测发现）。
  - **验证**：Rust 全 workspace 186 测试绿（audio 151，3 个 beatjump 测试按新语义改写）+ clippy -D warnings + bridge release 重建；Flutter analyze 零问题 + **69** widget 测试绿（新增 `leader_indicator_test.dart` 4 例：竖排对齐几何、单开 leader amber、双开 deck0/都关无、leader 切换后指示转移）；3 个 integration_test 依次单独跑全绿。**实机归用户自测**：任意位置按 beatjump 听感跳距 = N 拍、拍上按落点仍拍线；sync 单开/双开时 leader 轨 amber 指示。

- **P17 beatjump 落点量化回拍线 ✓（2026-08-15）**：用户反馈"beatjump 仍然不同步，但比之前好"——P16 去 snap 后跳距精确 N 拍（比之前好），但离拍起跳落点保留相位偏移 → 音乐继续抢/拖拍（不同步）。**P17：落点 snap 回最近拍线**（= 先对齐到当前拍，再从当前拍跳 N 拍）：在拍上/同步锁定（相位≈0）起跳时 snap 恒等 → **跳距仍精确 N 拍且落点仍在拍线**（P16 成果完整保留，P17 与 P16 仅离拍起跳处不同）；离拍起跳落点必在拍线。两约束（跳距精确 vs 落点在拍）在离拍位置不可兼得，按 DJ 惯例（Pioneer quantize）优先落点——跳距 ∈ [N−0.5, N+0.5] 拍。负跳（退拍）同样量化且确实后退。**测试**：4 个单测重写/新增——拍点起跳精确 4 拍两速率连续累计 4s（原 exact_distance 测试改拍点起跳）、离拍 1.3 跳 1 拍落点吸附 2.0（原"不吸附"断言翻转）、任意速率（0/30/−8%）离拍落点恒拍线 3.0、新 sweep 任意相位落点必在拍线且前移 + 负跳落 2.5；engine 级 2 测试 want0 改 snap 公式（0.5×round(x/0.5)，与 grid.snap 逐位一致）。验证：Rust 全 workspace **187** 绿（audio **152**）+ clippy -D warnings + 桥 release 重建；Flutter 侧零改动。实机自测：滑杆微调/波形点按（离拍）后按 beatjump，听感应立即回拍；拍上按时跳距听感仍精确 N 拍。

- **P17.1 beatjump 回滚精确跳距 + nudge 按钮互换 ✓（2026-08-15）**：用户反馈"更糟糕，因为有延迟导致 jump 后更慢"——P17 落点 snap 被否决：离拍起跳时落点吸附使实际跳距缩短最多 0.5 拍（例：相位 0.45 处按 ▶1 只跳 0.55 拍），叠引擎 560 帧延迟听感更慢。**回滚**：beatjump 恢复 P16 精确跳距 `pos + N×period`（P14 用户确认"跳跃时长 = 60/bpm×beats"即整拍 = 跳距，为最高优先语义）；P17 的 4 个 snap 测试全部回滚为 P16 三测试（151 audio 计数还原），engine 2 测试 want0 还原。**顺手项：nudge 按钮互换**（tempo_fader.dart）：◀◀ 由减速改**加速**（nudge +1）、▶▶ 由加速改**减速**（nudge −1）——引擎侧 `nudge>0 = NUDGE_UP` 不变，只换 UI 映射；tempo_fader_test 断言方向同步更新（◀◀→+1、▶▶→−1）。验证：Rust 186 绿（audio 151）+ clippy -D warnings + 桥 release 重建；Flutter analyze 0 + 69 widget 绿。实机自测：任意位置 beatjump 跳距听感 = N 拍；◀◀ 按住临时加速、▶▶ 按住临时减速（±8%）。

- **P18 三个新控件（ManualLoop/BeatJumpPanel/DeckFx）+ 布局重构完成 ✓（2026-08-15）**：用户要求——① manualloop（两行：÷2/当前拍数点击激活取消/×2，第二行 In/Out，实现同 pad beatloop 加 in/out 逻辑）；② beatjump（同款两行，in/out 改左右跳）；③ deck fx 取代 transport 行 FX 入口（强度旋钮 + on/off + 拍数显示和 ×2/÷2 + 名称点击切型）。布局：loop/jump 在 tempo 左侧、pad 上方、宽度平分；DeckFx 在 pad 右侧 tempo 左侧；tempo 高度 = pad+loop/jump+play/cue 总高；移除载曲控件改点击封面；nudge 临时加减速移到 play/cue 行（◀◀ 加速 / ▶▶ 减速，P17.1 互换保持）。
  - **P18.1 引擎（deck.rs）**：UI 写 `DeckN.loop_in/loop_out/loop_active` 总线，update_params 检测 loop_active 上升沿或激活期间 in/out 边界变化 → 进入 Capturing（真 buffer loop，复制 set_beat_loop 尾部逻辑：容量上限 min(64拍,30s)、anchor/frames 复用、loop-out 越界 fallback seek）。**零 FRB 改动**（wire 复用既有总线）。新增 2 测试：`manual_loop_bus_activation_enters_capture`（bus 激活 → playhead ∈ [-0.2, loop_out+0.05)、epoch 兜底 reset、underrun ≤ 256、FromBuffer）、`manual_loop_boundary_change_recaptures`（激活中改 bus loop_out → 重捕获）。loop_wrap_no_epoch_no_underrun 改重建函数体（epoch 不变、underrun 0）。
  - **P18.2 控件**（flutter/lib/widgets/）：**panel_button.dart**（新公共样式：0xFF2E353D 背景、圆角 4、fontSize 11 粗体白、lit 可选）；**manual_loop.dart**——拍数 1/32..64、默认 4 拍；中间按钮激活时显示实际环拍数（(loopOut−loopIn)×bpm/60）否则目标拍数，点击激活/取消（activateBeatLoop / setLoopActive(false)）；In/Out 写当前位置并激活，无有效对端自动回填默认拍长（In：out ≤ in → out = in + 默认拍长；Out：in 未设（=0）或 ≥ out → in = out − 默认拍长）；激活中 ÷2/×2 立即按新拍数重设（同 pad 语义）；**beatjump_panel.dart**——整拍 1..32 默认 4，◀/▶ onTapDown 按下即跳（P12 时序语义，跳距 = 本地拍数，P16 精确跳距引擎语义）；**deck_fx.dart**——槽 0：MixerKnob 强度（initFromBus drywet）+ Switch enable + 节拍行（仅 unit=="beats" 参数——目前 gate.period p1，其余 '–' 置灰）+ 名称 PanelButton 点击循环切型 0→1..8→0（写 manifest 默认值 p1..p4，同 RACK _selectFx）；状态经 waveTick 重读总线（FX pad 同款）；动作全部经 `PadActions` 出口（默认 EngineController/桥，测试注入假实现）。
  - **P18.3 布局**（deck_panel.dart）：行3 改 Row[Expanded(左列), SizedBox(8), SizedBox(72×kTempoFaderHeight, TempoFader)]；左列 = loop/jump 行（两 Expanded 平分）→ pad 行（Expanded(DeckPads) + Flexible(loose)(SizedBox(height:kDeckFxHeight, DeckFx))）→ transport 行（CUE/播放/NudgeButtons 104×30/Spacer）。常量 kLoopJumpPanelHeight=66、kDeckFxHeight=150、kTransportRowHeight=30、kTempoFaderHeight=258（=66+6+150+6+30）。_loadPressed 仅文件对话框，封面包 GestureDetector + Tooltip('点击载曲')；删 _pathCtrl/_expandHome/FX 按钮/fx_panel import。**tempo_fader.dart**：删 nudge 行/onNudge/kTempoFaderHeight；新增公共 **NudgeButtons**（◀◀→+1 加速、▶▶→−1 减速，按住 onTapDown/Up，默认 engine.setNudge）；推子区改 `_faderKey` GlobalKey + `_faderHeight()`（LayoutBuilder 不支持 intrinsic，P18 起父级定高不再需要 intrinsic 测量）。
  - **P18.4 布局坑（两个 unbounded 崩溃，机制已查明）**：**① Row 对非 flex 子节点传 maxWidth=∞**（flex.dart `_constraintsForNonFlexChild`——非 flex 子节点按内容收缩）——DeckFx 作为 pad 行非 flex 子节点收到 ∞ 宽 → 其内部 'ON' 行（Spacer）与节拍行（Expanded）"non-zero flex but incoming width constraints are unbounded"（24 异常）。修复：DeckFx 外包 **Flexible(fit: loose)**（有界宽 + 自然宽）而非定宽（pads 全弹性 4 列网格，定宽会挤 pad）。**② CustomPaint 无固有尺寸**：Expanded 给 TempoFader 推子区宽松跨轴约束 → CustomPaint 收缩到 0 宽 → GestureDetector 宽 0 → 拖拽/双击全部脱靶（测试 calls 空、双击不触发）。修复：CustomPaint 外包 **SizedBox.expand**。诊断法：Flutter 报 "nearest ancestor providing unbounded width" 的 RenderFlex 是**第一个有界宽的祖先**（其子链全无界），打印其 constraints + creator 精确定位。
  - **验证**：Rust 全 workspace 187 绿（audio 153：2 新测试 + 1 重建）+ clippy -D warnings + 桥 release 重建；Flutter analyze 0 问题 + **83** widget 绿（新增 manual_loop_test 5 / beatjump_panel_test 3 / deck_fx_test 6（fxBeatsParam/nextFxType/fxBeatSnap 纯函数 + 假 manifest 名称切型写默认值/'–' 显示/Switch）；tempo_fader_test 改 NudgeButtons 按住 ±1 松开 0、推子拖拽几何更新（区域 170 = 200−8−22）、双击回正；手动 loop Out 语义测试：未设 in（=0）算无有效 in 需回拉）；3 个 integration_test 依次单独跑全绿——**fx_panel_test 重写为 DeckFx 交互**（真引擎：名称点击 0→1 写默认值 0.375/0.35/0.3/0.0 → 连点循环到 8 门控 p1=1.0 → ×2/÷2 吸附 2→4→2 → 开关 enable 1→0 → 旋钮右拖 60px drywet≈0.43 → FX pad 右键仍开 RACK（pad 标签随选型显示门控，RACK 断言作用域到 FxPanel 避免与 pad 标签撞名）→ RACK 选回声同写总线）。**实机自测归用户**：In/Out 手感、beatjump 按下即跳、DeckFx 旋钮/切型听感、封面点击载曲、nudge 新位置。

- **P18.1 FX 布局重排（左旋钮 −150° 为 0 + 右侧两行三按钮）+ FX pad 占位 + DeckPanel 响应式完成 ✓（2026-08-15）**：用户要求——① fx 布局：左边旋钮（-150 度为 0），右边两行配置，第一行横向三个按钮调整 beat（参考 loop 样式），第二行三个按钮调整 fx 种类（左上一个 / 中间左键点击激活、右键弹出选择菜单 / 右下一个）；② pad fx 删功能只占位，fx 一 deck 只留一个通道；③ deck panel 响应式布局。
  - **P18.1.1 MixerKnob 线性全扫角（mixer_knob.dart）**：新增纯函数 `knobAngleLinear/knobValueLinear`——[min,max] 线性映射到 minAngleDeg..minAngleDeg+300°（300° = 5π/3，每侧 150° = 70px 拖动比例不变）。MixerKnob 加 `minAngleDeg` 参数（null = 现状 0 值恒指 12 点分段结构，EQ/gain 等含负值范围不受影响）。DeckFx 强度旋钮传 −150：0 值在 −150°（左端）、100% 在 +150°（右端）、50% 在 0°。painter 值弧/指示线/中心刻度/亮点全走 `_angle` 统一分支。
  - **P18.1.2 DeckFx 重排（deck_fx.dart）**：Row[左 MixerKnob 强度（minAngleDeg:-150）+ 右侧两行 Column]。行1（loop 同款样式）：÷2 / 拍数显示（点击回 manifest 默认拍数）/ ×2，无拍参数效果（type≠8）'–' 置灰不可点；行2：◀ 上一个 / 中间（label = 效果名，lit = enable：左键点击切换 enable、右键弹选型菜单）/ ▶ 下一个。新增 `prevFxType`（1→0→count→count−1 循环）；`_selectType` 统一选型路径（写 type + manifest 默认值 p1..p4；type 0 同时关 enable）；右键菜单 = showModalBottomSheet 列 '无效果' + 全部 manifest 效果（当前项打勾），选型复用 _selectType。PanelButton 加 `onSecondaryTap`（右键回调）。删 Switch/ON 行。
  - **P18.1.3 FX pad 只占位（deck_pads.dart）**：`_fxPad` 删 enable 切换与右键开 RACK，改 8 个 dead 占位 pad（label 'FX'，灰死不可点）；删 `PadActions.showFxPanel` 与 fx_panel import、_fxLabel/_safeBus 死代码。fx_panel.dart（旧 RACK）保留文件但 UI 无入口——选型菜单已取代其功能，参数调节如需可恢复入口。
  - **P18.1.4 DeckPanel 响应式 + 高度常量（deck_panel.dart）**：transport 行 NudgeButtons 固定 104 改 `Flexible`（窄窗先挤 nudge 再挤 Spacer，不溢出）；pads 文字包 `FittedBox(scaleDown)`（窄窗 '1/32' 等不溢出 pad）；高度常量更新——kDeckFxHeight 150→**56**（新内容 50 = 两行 22×2+6，旋钮列同高）、新增 kPadsRowHeight=116（26+4+40+6+40，行高决定者）、kTempoFaderHeight 258→**224**（=66+6+116+6+30）。
  - **验证**：Flutter analyze 0 问题 + **90** widget 绿（deck_fx_test 重写：新增 prevFxType / knobAngleLinear 逆映射 clamp / 中间左键 enable / ▶◀ 选型写默认值 / 右键菜单选型与选无效果关 enable；deck_pads_test FX 占位断言 9×'FX' + 点击右键无动作；deck_panel_layout_test 新增 240px 窄窗不溢出回归）；3 个 integration_test 依次单独跑全绿——fx_panel_test 重写：▶ 切型 1 写默认值 → 中间激活 1→0 → 连续 ▶ 到 8 门控 → ×2/×2/÷2 吸附 2→4→2 → 中间拍数点击回默认 1.0 → 旋钮右拖 drywet（slop 后 ~0.29 > 0.2）→ 右键菜单选回声重写总线 → FX pad 占位点击/右键不写 enable；deck_pads_test FX 段改占位断言；eof_playhead 不变。**实机自测归用户**：旋钮 0 值 −150° 观感与拖动手感、◀▶ 切型听感、中间右键菜单选型、窄窗口响应式表现。

- **P19 transport 行 6 等分按钮 + 未载入文案调整完成 ✓（2026-08-15）**：用户要求——① deckinfo 未载入文案改"点击左侧载入"，滚动波形删未载入文字；② play 行改 6 个按钮（shift 留空 / sync 点击开启 / cue 播放时点击暂停并回 cue 点 / play 原功能 / << >>），统一样式、前四个彩色、全部平分横向空间。
  - **P19.1 transport 行（新文件 transport_row.dart）**：6 等分按钮（Expanded + 4px 间距，高 30 = kTransportRowHeight 不变），统一样式（0xFF2E353D 底/圆角 4/fontSize 11 粗体/FittedBox 防窄窗溢出）：SHIFT（死键，0xFF1E232B 暗底 white12——未来 shift 组合功能预留）；SYNC（点击切换 setSync，开 = 0xFF3949AB 蓝底；P16 leader 判定移入 isSyncLeader 顶层函数，leader 轨 amber 边框 + 字色，双轨 syncOn 监听保持）；CUE（CueButton 改造，width 参数 null = 父约束撑满）；PLAY（播放/暂停文字切换，播放中 0xFF2E7D32 绿底）；<< >>（nudge 按住语义，P17.1 互换保持：<< 加速 +1 / >> 减速 −1，onDown/onUp 松写 0）。前四个状态色，<< >> 中性灰。动作全部经 PadActions（新增 setSync/setNudge 出口）。NudgeButtons 类删除（tempo_fader.dart），tempo_fader_test 对应段删除、测试移入 transport_row_test。
  - **P19.2 CUE 播放时点击 = 暂停并回 cue 点（deck_pads.dart CueButton._down recall 分支）**：seekExactTo(cue) 后追加 setPlaying(false)。**hotcue 保持"召回继续播"**（_hotcueDown 不动）——纯函数 nextCueAction 语义不变，暂停只发生在主 CUE 调用方；cue_action.dart 注释更新。
  - **P19.3 deckinfo/波形文案（deck_panel.dart / scrolling_wave_painter.dart）**：未载入 '未载曲' → '点击左侧载入'（点击封面载曲入口提示，P18 起封面即载曲入口）；滚动波形空态删文字只留居中播放头。SYNC 从 info 列移入 transport 行，_infoColumn 只留 KEY（_toggle 删 isLeader 参数；_isSyncLeader 移入 transport_row.dart 顶层）。
  - **验证**：Flutter analyze 0 问题 + **95** widget 绿（新增 transport_row_test 5 例：6 按钮等分文案 / SHIFT 死键 / SYNC 切换 / PLAY 文字切换 / << >> 按住 ±1 松开 0 / 160px 窄窗不溢出；leader_indicator_test 重写——SYNC 定位 transport 行 + KEY 在 info 列、amber 边框断言改 Container Border.top；deck_pads_test CueButton 播放段改断言 plays [(0,false)]；deck_panel_layout_test row1 列表删 SYNC）；3 个 integration_test 依次单独跑全绿（deck_pads_test CUE 播放中段加 getPlay() isFalse 断言——真引擎暂停回点验证）。Rust 引擎侧零改动。**实机自测归用户**：6 按钮等分观感与窄窗表现、播放中 CUE 暂停回点手感、SYNC 新位置与 leader 指示、点击左侧载入提示。

- **P20 transport 英文+顺序 / DeckFx 放大+开关+左键菜单 / 拍数分数显示完成 ✓（2026-08-15）**：用户要求——① '播放'改英文，顺序改 play cue sync shift；② fx 旋钮下方增加 on/off 开关，fx 名称按钮右键事件改左键单击，整体放大填满空间；③ loop 的 beat 数值只显示分数（1/2 1/4 等）或整数且只显示数字（去"拍"后缀、去小数与双源折算偏差——"明明是 4 拍显示 2 拍"）；beatjump 数值同样只显示数字。
  - **P20.1 transport 行（transport_row.dart）**：PLAY/PAUSE 英文化（播放中 0xFF2E7D32 绿底 PAUSE），顺序改 PLAY CUE SYNC SHIFT << >>（Expanded + 4px 间距 6 等分不变）。P19 语义全部保持：SHIFT 死键、SYNC 点击切换 + P16 leader amber 边框、CUE 播放中暂停回点（hotcue 召回继续播不受影响）、<< 加速 +1 / >> 减速 −1（P17.1 互换保持）。
  - **P20.2 DeckFx 放大 + 开关（deck_fx.dart）**：kDeckFxHeight 56→**116**（= kPadsRowHeight，填满 pad 行高）；布局左列 Column[强度旋钮 44（minAngleDeg:−150 线性 300°）+ 下方 Switch]，右列两行 height 28 / fontSize 12（PanelButton 加 fontSize 参数）；**enable 由开关负责**（删除旧中间按钮 enable 切换），名称按钮左键单击弹选型菜单（onSecondaryTap 删除，选"无效果"仍关 enable）。**响应式坑（P20.1）**：240px 窄窗 DeckFx 只分到 ~63px，左列（M3 Switch 固有宽 ~59 + 旋钮）固定宽放不下 → RenderFlex 溢出 5px（PanelButton FittedBox 治不了，溢出源是固定宽左列而非按钮文字）。修复：左列包 **Flexible(loose) + FittedBox(scaleDown)**——窄窗整体等比缩小、正常宽度零影响（Flexible 保证有界宽，直接 FittedBox 包整行会在 unbounded 下抛 flex 异常）。
  - **P20.3 拍数显示（manual_loop.dart 新顶层纯函数 fmtBeats + beatjump_panel.dart）**：`fmtBeats(b)` 去浮点噪声（×1e6 取整）→ 整数直显 / ≤32 任意整数分母分数（0.5→'1/2'、0.75→'3/4'、0.7→'7/10'）→ 无"拍"后缀；ManualLoop 激活中显示 cur = (loopOut−loopIn)/_beatSecs（_beatSecs = 60/(bpm>0?bpm:120)，与引擎 set_beat_loop 拍长同源——修复"4 拍显示 2 拍"的双源折算偏差）；BeatJumpPanel 中间按钮 '±N'。
  - **验证**：Flutter analyze 0 问题 + **96** widget 绿（P20 新增：fmtBeats 纯函数 11 例（整数/噪声/分数/0.7→'7/10' 兜底位数修正）、DeckFx 开关在旋钮下方 + 点击写 enable、左键菜单两测试改 tap 无 buttons、transport_row_test 顺序断言 PLAY<CUE<SYNC<SHIFT + PLAY/PAUSE 切换；240px 窄窗回归修复——溢出源定位法：takeException 吞详情 → 临时测试 debugDumpRenderTree grep OVERFLOWING 拿到 creator 链）；3 个 integration_test 依次单独跑全绿（fx_panel_test 更新：Switch tap ×2 enable 1→0、名称左键菜单、门控节拍 ×2/÷2、旋钮拖拽、FX pad 占位）。Rust 引擎侧零改动。**实机自测归用户**：PLAY/PAUSE 与顺序观感、DeckFx 放大后旋钮/开关/左键菜单手感、loop/beatjump 分数显示与 4 拍精度、240px 窄窗响应式。

- **P21 ManualLoop 手动定界（In/Out）+ BEATJUMP 成对横排 + DeckFx 填满完成 ✓（2026-08-15）**：用户要求——① loop 的 in/out 逻辑修改：手动确定循环上下界（不在 In 时确定），点击 Out 后整倍量化；② beatjump pad 内容排列改横向（<1 1> <2 2> 成对），页面滚动间隔 4；③ fx 右边填满、优化布局。
  - **P21.1 ManualLoop In/Out 重做（manual_loop.dart）**：**In 只写 loop_in = 当前位置**（不再自动回填 out、不再自动激活）；**Out 写 loop_out 并整倍量化**——有有效 in 时 `out = in + clamp(round((pos−in)/拍长), 1, ∞)×拍长`（长度恒为整拍、循环与节拍对齐；不足 1 拍保底 1 拍，不出现零长环）；无有效 in（未设 = 0 或 in ≥ pos）时无量化基准 → out 原位 + 回拉 in = pos − 默认拍长（防御分支保留）；Out 后激活（引擎 bus 边沿检测进捕获，**引擎零改动**）。注释语义链：手动定界 = In 定起点、Out 定终点+长度整拍量化，Pioneer 风格。
  - **P21.2 BEATJUMP pad 成对横排（deck_pads.dart）**：显式页表（_beatjumpPages 上排全左跳/下排全右跳）改**线性列表成对横排**（◀1 ▶1 ◀2 ▶2 ... ◀32 ▶32，12 项）——复用 LOOP 列表机制（窗口 8 步长 4）：页0 = 左右 1/2/4/8、页1 = 滚动 4 项（4/8/16/32，◀4 仍可见）；_pageCount 删 beatjump 特殊分支走通用窗口公式（12 项 → 2 页）。onTapDown 按下即跳（P12）与引擎跳距语义不变。
  - **P21.3 DeckFx 填满右侧（deck_panel.dart）**：pad 行 `Expanded(DeckPads) + SizedBox(2) + Flexible(loose)(DeckFx 自然宽)` → **`Expanded(flex:3)(DeckPads) + SizedBox(2) + Expanded(flex:2)(SizedBox(116, DeckFx))`**——fx 拿固定份额填满左列行尾（不留白），pads 3/5、fx 2/5；窄窗防御沿用 P20.1（左列 Flexible+FittedBox 等比缩小）+ P18.1（pads 文字 FittedBox）。
  - **验证**：Flutter analyze 0 问题 + **98** widget 绿（manual_loop_test 重写：In 只写 in 不激活/已有 out 不动、Out 整倍量化 2 例（3.6 拍→4 拍 out=12.0、0.28 拍保底 1 拍 out=10.5）、无 in 回拉保持；deck_pads_test beatjump 改横排断言（◀1.x<▶1.x<◀2.x<▶2.x 成对相邻）+ 页1 滚动 4 后 ◀4 仍在；deck_panel_layout_test 新增 fx 填满断言——fx 右缘距面板右缘 = tempo 72+间距 8+padding 10+margin 6 = 96±2，即 fx 顶到左列行尾）；3 个 integration_test 依次单独跑全绿（deck_pads BEATJUMP 翻页断言 ◀16/▶32 在页1 滚动后仍成立，无需改）。Rust 引擎侧零改动。**实机自测归用户**：In 定起点/Out 整拍量化的听感（离拍 In + 半拍 Out → 应听到整拍循环）、beatjump 成对横排与翻页手感、fx 加宽后的旋钮/开关布局观感。
- **整理：GitHub 提交准备完成 ✓（2026-08-15）**：改名 HyperMixx + 迁移 ~/Workspace/HyperMixx + git 管理（branch main，remote windup-bird/HyperMixx）+ 中文 README。
  - **timestretch 切回 crates.io 0.11.0**：原 path 依赖（本地克隆 master，含 0.11.0 后 DSP/分析修复）随目录清理误删；恢复克隆后因 gitlink 无法作为提交规范纳入 repo（gitlink 提交远端得到空目录），用户决策**切回 crates.io 0.11.0**（本机 registry 有缓存，离线可用）。代价：丢失 master 修复——**segment.rs 的 `detect_beats_multi`（双分辨率：12k 粗链定 tempo 层级 + 48k superflux 定拍位）改为 0.11.0 `detect_beats_with_options`（单分辨率 48k + EDM hint_range 100–160 + rigid 细化不变）**；keylock 引擎 API 一致（注释已预告"可随时切回"）。验证：cargo test 188 绿（P10.2 的 segment ±0.3 BPM/±10ms 用例原样通过）+ clippy -D warnings 干净 + flutter analyze 0 + 98 widget 绿 + 3 integration 依次绿。**实机自测归用户**：beatgrid 精度（双分辨率→单分辨率粗链层级丢失，八度定级可能略变）、keylock 音质与 0.11.0 前体验对比。
- **P22 loop/beatjump 消卡顿完成 ✓（2026-08-15）**：用户报告 loop 每圈 click + ManualLoop 激活 33ms 静音 + beatjump 首块欠载，并提议"改用 timestretch 实现"。三路探查结论：**timestretch 已用满**（loop = 官方无 reset 环绕宿主模式连续喂入 + set_track_position 重锚；beatjump = reset + warm_start(1) 最小预卷；0.11.0 无未用 API，master 与 0.11.0 差异全在分析侧）——**不切版本，三个卡顿源逐个消**。
  - **P22-A 圈首接缝等功率交叉淡化（deck.rs LoopBuf）**：wrap 时 loop_out 帧后直接 loop_in 帧，波形任意不连续 → **blend 缓冲预计算 192 帧（4ms@48k）圈尾×圈头等功率交叉淡化（cos²+sin²=1）**；`feed_from_loop_buf` cursor 落在 blend 区时喂 blend 切片、否则照旧喂 data——**cursor 语义不变**（blend 只是 data 该处 192 帧的替代物，一圈仍恰好 n 帧），播头公式/loop_sp_anchor/P11.1 退出锚点零影响。
  - **P22-B reader 回填 + 偏移入环（caching_reader.rs + deck.rs）**：ManualLoop 激活首圈必不完整（In 按下时播放头已过 loop_in）→ 每次激活 `loop_wrap` 兜底全预卷 reset（1584 帧 ≈33ms 静音 + reader 重 seek）。修复：激活边沿按 **out_past 分支**布防——① 播放头**已过量化 out** → **全圈回填布防**（Backfill 命令让 reader 解码 [loop_in, loop_out) 推侧环；完成后**立即带偏移入环**：cursor = (feed_pos−loop_out×sr) mod loop_frames，播放位置不跳回、不等圈界，从当前相位无缝映射入环，feed_tail 尾随缓冲重建入环接缝 blend）；② **未到 out** → **部分回填**（Backfill [loop_in, feed_pos)，捕获从尾部续写）播到量化 out 再回绕。回填经独立 SIDE_RING（256 chunk 4MB）排空到环前区，**音频线程零分配**；回填迟到/reader 死亡 → 既有全预卷 reset 兜底（loop_back 测试固化）。
  - **P22-C beatjump ring 窗口保留（deck.rs seek_internal）**：seek 清空整个 chunk ring + clear_cache，落点 100% 依赖 reader 重解码（1–5ms 欠载静音）→ **智能排水 + 保留前缀**：pop 全部 end ≤ read_frame 的陈旧 chunk；环非空且 front.start ≤ read_frame → 保留整个余环（ring 内容恒为连续区间，落点在窗内 → 零欠载），`ReaderCmd::Seek{resume}` 通知 reader 续推不重 seek；后退跳/超窗 → 全排旧行为。**RING_CAPACITY 64→256**（2.73s→10.9s 预解码窗口，±8/16 拍跳基本命中）；preserve 双路接受规则（旧 epoch 窗口 + 新 epoch 并存，首个新 chunk 到达即清）。
  - **P22-D Flutter onTapDown**：manual_loop.dart `_toggle`/`_setIn`/`_setOut` + deck_pads.dart `_loopPad` onTap → **onTapDown**（同 beatjump P12 先例）——loop 激活瞬间的播放头位置即捕获起点，onTap 等手势仲裁 ~数十毫秒位置就偏了（P22 卡顿源 2 的 UI 侧镜像）。÷2/×2 改本地拍数不敏感保持 onTap。
  - **验证**：cargo test --all-targets **163** 音频（P22 新增 7 例：loop_wrap_seam_blend_no_click 锯齿波圈界跳变 max_delta 断言、loop_offset_entry_seam_no_click、manual_loop_bus_activation_enters_capture 改写回填语义、manual_loop_out_past_enters_with_offset_immediately、loop_offset_entry_exit_resumes_at_advanced_feed_pos、beatjump_lands_in_preserved_window_no_underrun、beatjump_beyond_window_still_requires_refill 负控 + 3 真 reader 例）+ bridge 11 + core 10 绿；clippy --all-targets -D warnings 干净；flutter analyze 0 + **98** widget 绿（onTapDown 改法用例不变）；3 个 integration_test 依次单独跑全绿（真引擎联动）。引擎 beatjump 测试改动态延迟契约（落点在保留窗内延迟 272 帧、超窗 560 帧，断言 (0, 0.03] 区间）。注意：`cargo fmt --check` 基线漂移（未纳入验证门，全仓库 15+ 文件提交时就已不干净，本次不动）。**实机听感归用户自测**：loop 每圈接缝 click 是否消失、Out 已过量化点"立即带偏移入环"手感（不跳回、相位映射自然）、Out 未到量化点"播到 out 回绕"平滑度、beatjump 窗内跳零卡顿。
- **P22.1/P22.2 DeckFx 布局与 DeckPanel 响应式完成 ✓（2026-08-15）**：用户连续两轮 UI 调整。
  - **P22.1 DeckFx 去留空（deck_fx.dart + mixer_knob.dart）**：① 删'强度'label——MixerKnob 空 label 不渲染文字；② 按钮行高 28→**42**（×1.5，`kFxRowHeight`）；③ 行1/行2 紧贴（`SizedBox(6)` 原方式、不撑满 116）；④ 左右列上下边界**严格对齐**：左列高 = `kFxModuleHeight` = 90（= 2×42+6），旋钮贴顶/开关贴底 ↔ 行1 顶/行2 底；⑤ **Switch shrinkWrap**：M3 padded 高 48 使 44+48=92 溢出 90（2px），shrinkWrap 高 32 且边界仍对齐。测试断言改 PanelButton 定位（文字居中 ≠ 按钮位置）：行1顶=旋钮顶、行2底=开关底、行距 = 42+6，容差 <1px。
  - **P22.2 DeckPanel 响应式（deck_panel.dart）**：pads:fx 固定 3:2 → **LayoutBuilder 动态切换**——可用宽 ≥ `kFxWideThreshold`(560) → **2:3**（fx 60% = 40%×1.5，fx 按钮宽度拉长 1.5 倍）；更窄回退 3:2（fx 60% 会把 pad 格压成竖条；240 极窄窗维持 P20.1/P18.1 FittedBox 防御）。阈值 560 时 pads = 224（4 格 ≈48px 近方形）仍可用。
  - **验证**：flutter analyze 0 + **99** widget 绿（deck_fx_test 边界对齐断言重写；deck_panel_layout_test 新增 400 中窗回退 3:2 断言 + 700 宽窗 fx = pads×1.5 断言）。**实机观感归用户**：宽窗 fx 按钮更大更好点、窄窗 2:3→3:2 切换的观感、旋钮/开关/按钮对齐。
- **P22.3 pads:fx 固定 1:1 + fx 按钮横向铺满完成 ✓（2026-08-16）**：用户要求——① pad 与 fx 宽度 1:1（删除 P22.2 阈值切换）；② fx 的 beat/select 两行按钮横向宽度铺满内容。排查发现"按钮没铺满"根因是 **P20.1 起遗留的 Flexible(flex:1, loose)**：左列以 loose 参与 flex 分配，约束 = 半列宽，但 FittedBox 只取 60px 固有宽 → 右列 Expanded 只拿到剩余一半（220px 宽时实际 106 vs 应得 152，留白 46px）。修复（deck_fx.dart）：**LayoutBuilder 直出**——删掉包在外面的 Row（flex 行给非 flex 子 **unbounded 主轴向约束**，内层 Expanded 报 "RenderFlex children have non-zero flex but incoming width constraints are unbounded"，调试探针确认 `0.0<=w<=Infinity`），LayoutBuilder 从 SizedBox 拿到 tight 220×116；左列上限 = **min(60, 半列)**：宽窗 60 固定（右列 Expanded 拿满剩余全部宽度，×2/▶ 右缘贴 fx 列右缘 <1px 断言）、窄窗两列等比收缩（`((maxWidth−8)/2).clamp(0,60)` 保证右列恒 ≥ 左列，240px 窄窗不溢出——固定 60 的方案在 63px 列宽下会算出负 flex 空间溢出 8px，min(60,半列) 兼治两窗）。
  - **验证**：flutter analyze 0 + **99** widget 绿（deck_fx_test 补铺满断言：行1 尾按钮 ×2 / 行2 尾按钮 ▶ 右缘与 DeckFx 右缘差 <1px；deck_panel_layout_test 700px 用例改断言 pads:fx 等宽 ±4px + 新增 400px 中窗 1:1 不溢出用例；240px 窄窗回归保持）。Rust 引擎侧零改动。**实机观感归用户**：1:1 后 pads 与 fx 并排观感、按钮铺满后的行尾对齐、窄窗缩放。
